# Implementation Status

Last updated: 2026-08-20

Legend:
- `[x]` implemented and currently wired into the app
- `[~]` partially implemented, local-MVP only, or intentionally narrow
- `[ ]` not implemented

This file is the current handoff map for humans and orchestrator agents. When code changes add, remove, or materially alter behavior, update this file and the relevant README/docs section before finishing the implementation.

For product-level capability grouping and command-surface consolidation, read `docs/08-product-capability-audit.md`.
For route ownership, split-brain risks, and the server extraction plan, read `docs/09-architecture-inventory.md`.

## Documentation Maintenance Rule

After every implementation pass:
- update `README.md` when commands, env vars, setup, API routes, or manual testing steps change
- update the relevant `docs/*.md` file when architecture, behavior, ontology, risk policy, profile fields, or roadmap status changes
- update this status file when a feature moves between not implemented, partial, and implemented
- keep status labels honest; do not mark a feature implemented just because a placeholder or stub exists
- run the normal validation command set when code changed: `pnpm typecheck`, `pnpm build`, and `pnpm test` when relevant

## Core Platform

- [x] TypeScript pnpm monorepo with `packages/core`, `packages/db`, `packages/llm`, `apps/api`, `apps/telegram-bot`, and `apps/worker`
- [x] Fastify API over Prisma/Postgres
- [x] Telegram-first channel adapter
- [x] Channel-agnostic `NormalizedInboundMessage` abstraction
- [x] Intent routing before business logic
- [x] Conversation-first UX parity for natural help, setup, daily/weekly review, planning, hygiene, goals/actions/memory, integration guidance, explicit email-rule enable requests, and explicit integration sync requests
- [x] Channel-neutral operator attention state/API: `GET /users/:userId/operator-attention` composes actions, goals, events, risks, hygiene, Gmail reviews, and latest weekly-review status for natural attention/email-attention questions and operator-loop surfaces
- [x] Optional LLM Semantic Router v4 for normal free-text understanding after deterministic safety/command/pending handling and before generic chat fallback; targets English/Spanish/Catalan phrasing, returns structured intent only, keeps short-lived Gmail-rule conversation context, and leaves all mutations to deterministic API executors
- [~] Future-integration observation boundary documented in `docs/09-architecture-inventory.md`: Gmail reviews are today's concrete source object, but downstream decisions should stay generic (`create action`, `log goal evidence`, `ask clarification`, and later `suggest calendar event`) so Calendar or other app observations can plug into the same V3 tool/action layer without Gmail-only branches. Calendar is not implemented.
- [x] **Conversation Orchestrator v2 — retired.** Phase 1 shipped an operation-planning architecture (`ConversationContext`, `AvailableOperations`, an OperationPlanner contract, deterministic validation/execution, execution-result response composition) behind the `CONVERSATION_ORCHESTRATOR_V2_ENABLED` flag on `/messages/process`, plus a debug-only `/messages/process_v2` endpoint that exposed it unconditionally (retired earlier — zero production callers). A later audit could not prove from repository inspection alone that the flag was safe to delete (it could only confirm no *tracked* repo config set it, not that no live deployment did); product then confirmed no live deployment relied on it, and the whole branch plus its `apps/api/src/conversation/{orchestrator-v2,context,operation-catalog,operation-planner,operation-validator,operation-executor,response-composer}.ts` modules and its 32-test file were deleted. `/messages/process` now runs only the legacy deterministic/semantic pipeline that was already its fallback — no behavior change for any in-use scope. Agent Runtime v3 (`POST /agent/message`) remains the default normal-chat runtime and is unaffected. See `docs/09-architecture-inventory.md`'s "Conversation Orchestrator v2 — Full Retirement" for the deletion record.
- [~] Agent Runtime v3 (`apps/api/src/agent-runtime/`, `POST /agent/message`): isolated LLM-plan/deterministic-validate/deterministic-execute runtime, now the **default** runtime for normal (non-command) Telegram chat — `TELEGRAM_AGENT_RUNTIME_V3_ENABLED=false` is the explicit opt-out back to the legacy `/messages/process` pipeline; slash commands and Gmail OAuth/setup/sync are unaffected either way. Session state (topic, pending confirmation, visible entities, recent mutations, bounded history) is persisted per `userId`+`channel` in `AgentConversationSession` with a sliding 24h TTL, so it survives an API restart. A per-user in-memory lock serializes same-user turns. Tool catalog covers memory, progress-event logging, action list/create/snooze/complete/archive, bulk/multi-item action-hygiene cleanup (`action.hygiene_start`/`action.hygiene_apply` — "clean up my actions", numbered multi-decision replies like "complete 1, snooze 2 to Friday, archive 3", resolved deterministically against the session's visible entities, never hidden LLM memory; see `docs/09-architecture-inventory.md`'s "Action Hygiene Migrated Into Agent Runtime v3"), next-week/current-week planning (`planning.next_week_start`/`planning.next_week_edit`/`planning.next_week_show_current`/`planning.next_week_apply` — "plan next week", numbered draft plan, edits before confirming via natural references ("remove the YouTube one", "move the gym one to Friday", "make it lighter") with numbered fallback ("remove 2", "change 1 to Tuesday") still supported, "show me the plan" re-displays the current draft without regenerating it, "yes"/"looks good" to create; see `docs/09-architecture-inventory.md`'s "Next-Week Planning Migrated Into Agent Runtime v3", "Next-Week Planning Hotfix — Deterministic Edits and Natural-Language UX", and "Next-Week Planning V3 State-Machine Hardening" — the latter's multi-turn bugs are the reason a scripted conversation smoke/eval harness now exists, see "V3 Scripted Conversation Smoke/Eval Harness"), weekly review (`weekly_review.start`/`weekly_review.save` — "review my week", "how did this week go?", "what should I improve next week?" all show a grounded review built from real goals/actions/events/email activity, "save this review" persists it, "cancel" clears it; see `docs/09-architecture-inventory.md`'s "Weekly Review V3 Migration"), Gmail status/rule-create/rule-list/rule-explain/review, and Gmail rule pause/resume/removal (`gmail.rule.propose_update`/`gmail.rule.apply_update` — "turn off X", "pause the X rule", "resume X", "delete/remove the X rule", resolved by name against a fresh DB lookup, always confirm-gated; see `docs/09-architecture-inventory.md`'s "Gmail Rule Management V3 Migration") — it does not yet cover changing an existing rule's review-only/auto-log behavior (not existing domain behavior — fixed at rule creation only) or daily loop settings, both of which remain legacy-only (see `docs/09-architecture-inventory.md`'s "Legacy conversation stack to remove or isolate" for the full breakdown). **Strategy update:** capability migration into v3 (not `server.ts` line-count reduction) is now this project's primary cleanup goal — see `docs/09-architecture-inventory.md`'s "Strategy Change — Capability Migration Into Agent Runtime v3". New conversation/product work should go into v3, not the legacy pipeline.
- [x] Agent Runtime v3 Gmail connect/reconnect/status UX: normal chat phrases such as `connect Gmail`, `connect my email`, `integrate email`, `reconnect Gmail`, `send me the Gmail link`, and contextual `send me link to reconnect it` route to `gmail.status` with the real readonly OAuth URL when configured. Expired Gmail auth is presented as a reconnect action first, active rules are secondary context, and the status tool never runs Gmail sync.
- [x] Agent Runtime v3 Gmail nudge preference UX: `turn on Gmail nudges` / `stop Gmail nudges` route to `proactive.settings_propose_update`, open the existing yes/cancel confirmation flow, and never trigger a Gmail sync. If Gmail is disconnected or expired, the proposal and confirmed reply explain that nudges are enabled but cannot deliver until Gmail is connected/reconnected.
- [x] Architecture inventory v1 in `docs/09-architecture-inventory.md`: entry points, routing order, route ownership map, `server.ts` map, split-brain risks, ConversationContext audit, LLM OperationPlanner audit, and risk/guardrail generalization plan
- [~] API route modularization started beyond conversation helpers: `/messages/process` Fastify registration now lives in `apps/api/src/routes/messages.ts`; the actual process handler still depends on `apps/api/src/server.ts` helper glue until more surfaces are extracted. Server cleanup phase 1 additionally extracted `apps/api/src/server-types.ts` (39 pure types) and two genuinely zero-dependency route groups, `apps/api/src/routes/memory.ts` and `apps/api/src/routes/notification-settings.ts` — all behavior-preserving pure moves, `server.ts` down to 17,380 lines from 17,903. Server cleanup phase 2 extracted the check-in prompt and free-text ingestion routes into `apps/api/src/routes/checkins-ingest.ts` (`GET /users/:userId/checkins/daily/prompt`, `POST /users/:userId/ingest/text`, `POST /users/:userId/ingest/job-search-text`, plus their two exclusively-used private helpers `ingestText`/`composeIngestionReply`) and de-duplicated the `isRecord` type guard — previously defined privately in `server.ts` and called 53 times there — into `apps/api/src/utils/records.ts` so the new route module doesn't need a circular import back into `server.ts`; `server.ts` down to 17,293 lines. Server cleanup phase 3 extracted the daily/weekly insight routes into `apps/api/src/routes/insights.ts` (`GET /users/:userId/insights/daily`, `GET /users/:userId/insights/weekly`, plus their exclusively-used private helpers `maybePolishInsight`/`isDirectInsightProfile`/`startOfToday`/`startOfLastSevenDays`/`parseDateStart`/`addDays`) and de-duplicated `shouldUseOpenAIAnalysis` into `apps/api/src/utils/env.ts` for the same reason `isRecord` moved in phase 2; `server.ts` down to 17,172 lines. The `pending-actions` route group was inspected and skipped this pass because its confirm route's only risky dependency, `applyPendingAction`, is a ~345-line function entangled with Gmail rule management and action-hygiene batch operations (both already-flagged high-risk zones) and is also called from the legacy `/messages/process` chat-confirmation flow, not just this route — extracting it now would either drag those tangles along or force a circular import. Server cleanup phase 4 retired `/messages/process_v2` entirely (debug-only, zero production callers) — its route/handler were deleted, `runConversationOrchestratorV2ForMessage`'s now-dead `returnUnhandled` option was removed with it, and `tests/conversation-orchestrator-v2.test.ts` (34 tests) was rewritten to exercise the same Conversation Orchestrator v2 logic through `/messages/process` (with the flag forced on) instead — 32 tests remain, 2 were removed as testing debug-route-only behavior with no production equivalent; `server.ts` down to 17,122 lines. Server cleanup phase 5 retired Conversation Orchestrator v2 itself — the `CONVERSATION_ORCHESTRATOR_V2_ENABLED` branch and its 7 `apps/api/src/conversation/` modules were deleted (product confirmed no live deployment used the flag), along with the 32-test file and several now-orphaned `server.ts` helper functions that existed only to wire into v2's callback interface; a new 3-test regression file (`tests/messages-process-legacy.test.ts`) confirms `/messages/process` still works through the remaining legacy path and that Agent Runtime v3 is unaffected; `server.ts` down to 16,938 lines. Phase 6 deleted the now-unreachable `packages/llm/src/operation-planner.ts` (the LLM-calling half of v2's operation planner, flagged but left out-of-scope by phase 5) and its export from `packages/llm/src/index.ts`; no `server.ts` changes. While tracing that deletion, `packages/core/src/conversation-orchestrator.ts` was found to be fully unreachable too, but was left alone that pass — a different, more widely-depended-on package than that pass's scope covered. Phase 7 re-verified and deleted it: all 19 exported symbols (schemas, types, interfaces, `normalizeConversationOperationPlan`) confirmed zero consumers anywhere via word-boundary-anchored search, its export removed from `packages/core/src/index.ts`, and `pnpm typecheck` run across all 6 workspace packages to confirm nothing downstream broke — this closes out the v2-deletion chain that started in phase 5. Agent Runtime v3's own types (`apps/api/src/agent-runtime/types.ts`) and planner (`apps/api/src/agent-runtime/planner.ts`) are unrelated, independent implementations and were never affected by any of phases 5–7. Phase 8 began the Gmail-rules extraction queued after phase 3: mapped ~90 Gmail-related functions in `server.ts` into OAuth/token/provider-API, sync-worker, legacy conversation/semantic-router NL handling, and pure rule-management, then extracted only the last group into `apps/api/src/gmail/gmail-rule-service.ts` (`isBuiltInEmailAdapter`, `EmailRuleHumanDisplayGroup`, `groupEmailRulesForHumanDisplay`, `formatGmailEmailRuleSelectionLines`, `getVisibleGmailEmailRules`) — this closes the Gmail-side half of what was blocking `applyPendingAction`'s `custom_email_rule` branch, which turned out to depend on exactly one server.ts-local helper once traced (everything else it called was already a clean `@operator-agent/db` or `conversation/gmail-autonomy.ts` import). `normalizeForComparison` (58 unrelated call sites) was also de-duplicated into `apps/api/src/utils/text.ts`, following the same pattern as `isRecord`/`shouldUseOpenAIAnalysis` in earlier phases. The legacy conversation/semantic-router Gmail cluster (rule editing, pronoun/target resolution) was inspected and deliberately left in place — tangled with free-text NL parsing, not pure rule management. `server.ts` down to 16,875 lines; 6 new tests in `tests/gmail-rule-service.test.ts`. Action-hygiene (`applyActionHygieneBatchOperations`) was the sole remaining tangle blocking `pending-actions`. Phase 9 closed it: `applyPendingAction`'s `action_hygiene` branch turned out to depend on just `readActionHygieneBatchOperations` and `applyActionHygieneBatchOperations`, not the much larger session-creation/candidate-analysis/NL-parser surface — both moved into `apps/api/src/actions/hygiene.ts`, with their two generic-but-shared dependencies (`createGoalProgressFromCompletedAction`, 5 call sites; `formatLocalDateTime`, 17 call sites) de-duplicated into `apps/api/src/actions/goal-progress.ts` and `apps/api/src/utils/datetime.ts` respectively, same pattern as every prior phase. The action-hygiene session-creation cluster and legacy NL-reply parser were inspected and deliberately left in server.ts — they depend on 9 more generic-but-currently-local helpers (`getUserTimezone` alone has 43 call sites) that `pending-actions` never actually needed. `server.ts` down to 16,727 lines; 6 new tests in `tests/actions-hygiene.test.ts`, including an end-to-end HTTP test of the `action_hygiene` pending-action confirm flow. `applyPendingAction`'s three previously-tangled branches (`custom_email_rule`, `gmail_autonomy_preference`, `action_hygiene`) are now all fully clean — `pending-actions` route group extraction is the recommended next pass. Phase 10 completed the chain: extracted `GET /users/:userId/pending-actions`, `POST .../confirm`, `POST .../reject` into `apps/api/src/routes/pending-actions.ts`, moving `applyPendingAction` (~345 lines) and `findPendingAction` alongside them. Re-tracing `applyPendingAction`'s dependencies fresh surfaced three more generic-but-shared helpers needing the same de-duplication treatment as every phase before: `arrayOfStrings` (20 call sites) → `apps/api/src/utils/arrays.ts`, `getUserTimezone` (43 call sites, the most widely-shared helper in this whole series) → `apps/api/src/utils/user-timezone.ts`, and `createCustomGoalProgressEvent` → a second export on `apps/api/src/actions/goal-progress.ts`. `applyPendingAction` is exported (not fully private) because `server.ts`'s legacy `/messages/process` confirmation branch still calls it — a one-directional, non-circular import back from `server.ts`. Agent Runtime v3's legacy `PendingAction` interop (which detects/defers/safely-cancels but never executes `applyPendingAction`) is unaffected — all 6 of its tests pass unchanged. `server.ts` down to 16,289 lines (−438, the largest single-pass reduction in this series); 8 new tests in `tests/routes-pending-actions.test.ts` covering every pending-action type touched plus 404/400 edge cases. Phase 11 re-inspected the legacy Gmail natural-language cluster phase 8 deliberately left in place, and found the "too tangled" call was stale: the pronoun/target-resolution machinery it was blocked on had already been extracted to `apps/api/src/conversation/email-rule-selection.ts` in an earlier pass. With that piece already clean, the remaining ~48 functions/types (setup/status/capability formatters, autonomy-preference NL parsing, custom-rule proposal building, rule editing/question-answering/management, target/context resolution) all traced to already-extracted modules, `@operator-agent/core`/`@operator-agent/db`, or pure in-cluster helpers — confirmed by grepping every candidate function name repo-wide and finding zero callers outside `server.ts` itself. All 48 moved into `apps/api/src/legacy/gmail-conversation.ts`. Six more generic-but-shared helpers were de-duplicated along the way, same pattern as every phase before: `sentenceLikeTitle`/`pendingEmailReviewLine`/`isConfirmationMessage`/`isRejectionMessage` → `utils/text.ts`, `stringFromRecord` → `utils/records.ts`, `pendingDecisionExpiry` → `utils/datetime.ts`, `uniqueStrings` → `utils/arrays.ts`, `archiveStaleJobSearchEmailRules` → a second export on `gmail/gmail-rule-service.ts`, and `getPendingEmailReviewCount` → `packages/db/src/index.ts`. Only the Gmail sync-triggering functions (`syncGmailForConversation`, `syncIntegrationsForConversation`, and their exclusive formatters — they call `syncGmailConnection` directly) and the three shared, non-Gmail-specific legacy dispatchers (`handleSemanticRouterIntent`, `detectDeterministicSemanticRouterIntent`, `detectConversationSurfaceIntent`) stayed in `server.ts`, now importing the extracted module. `server.ts` down to 14,292 lines (−1,997, the largest single-pass reduction in this series); no new tests needed — every path this pass moved is already covered end-to-end by existing `/messages/process` and `/pending-actions/:id/confirm` HTTP tests, which exercise the public route surface rather than server.ts-private functions. All 224 existing tests pass unchanged; Agent Runtime v3's Gmail handling (fully independent) is unaffected. Phase 12 closed the action-hygiene blocker phase 9 deliberately left in place: the session-creation/analysis cluster (`server.ts` lines 2585–2816) and the free-text reply parser (lines 13225–14192) — 34 functions/types total — moved into `apps/api/src/legacy/action-hygiene-conversation.ts`. This turned out to be a bigger surface than "NL/messages-process only": the session-creation zone is the direct backing implementation for two real HTTP routes, `GET /users/:userId/actions/hygiene` (confirmed via `apps/telegram-bot/src/index.ts:1009-1028` to be what Telegram's `/action_hygiene` and `/debug_action_hygiene` slash commands call) and `POST /users/:userId/actions/hygiene/reply` — both routes stayed registered in `server.ts` unchanged, only their underlying function bodies moved. A shared "pending action candidate" selection/formatting layer (`PendingActionCandidate` type, `toPendingActionCandidate`, `selectPendingActionCandidate`, and 7 more) was discovered mid-mapping to be used by both the hygiene cluster and server.ts's own separate, not-extracted `action_target_clarification` handling, so it got its own new module, `apps/api/src/actions/pending-candidate.ts`, rather than living inside the hygiene-specific one. Six more generic-but-shared helpers were de-duplicated the same way as every phase before: `getDateTimePart`/`formatDateInTimezone`/`daysBetweenLocalDates`/`daysBetween` → `utils/datetime.ts`, `normalizeComparableText`/`ordinalSelectionIndex` → `utils/text.ts`, plus two brand-new single-purpose files matching this repo's established granularity, `utils/action-item.ts` (`isSnoozedDue`) and `utils/goal-priority.ts` (`goalPriorityRank`). One extraction mistake was caught and corrected mid-pass: dead code `looksLikePendingDecisionReply` was initially moved along with the parser cluster (it calls `parseActionHygieneReply`) but also calls `parseNextWeekPlanReply`, an unrelated next-week-plan function — it was moved back to `server.ts`, which now imports `parseActionHygieneReply` from the new module instead of the reverse. `server.ts` down to 13,106 lines (−1,186); no new tests needed — every path this pass moved is already covered by existing tests across `tests/actions-hygiene.test.ts`, `tests/routes-pending-actions.test.ts`, `tests/agent-runtime-legacy-pending-action.test.ts`, `tests/agent-message.test.ts`, and `tests/email-review-dedupe.test.ts`, all HTTP/route-level. All 224 tests pass unchanged; Agent Runtime v3's single-item `action.*` tools are untouched. Phase 13 extracted the planning session handlers queued after phase 12: the next-week/current-week planning cluster (`server.ts` lines 3592–4841, ~40 functions/types, immediately adjacent to the separate weekly-review feature it's easy to mistake it for). Mid-mapping surfaced a hard, unavoidable blocker: `buildNextWeekPlanContext` — the context builder every suggestion/formatting/reply function takes as an input — itself calls `buildWeeklyReviewContext`, weekly review's own ~500-line context builder, explicitly out of scope for a planning-only pass. `resolveNextWeekPlanReply` and `createPlanForConversation` (the `/messages/process` "plan next week" entry point) both call `buildNextWeekPlanContext` directly too, inheriting the same blocker. All three stayed in `server.ts`. Everything else in the cluster — 37 functions/types operating on an already-built context/payload rather than building one — moved into `apps/api/src/legacy/planning-conversation.ts`: deterministic + optional-LLM suggestion generation, pending-plan formatting, the free-text reply parser, and planning-request detection. The two `next-week-plan` HTTP routes (the backing implementation for Telegram's `/plan_next_week` slash command, confirmed via `apps/telegram-bot/src/index.ts:1622-1641`) stayed registered in `server.ts` unchanged, now importing from the new module. Three more generic-but-shared helpers were de-duplicated the same way as every phase before: `addDaysToLocalDateString`/`localDateStartUtc` → `utils/datetime.ts`, `sharesMeaningfulToken` → `utils/text.ts`, and the `PlanWindowKind` type moved fully into `server-types.ts` (it was previously exported from `server.ts` only so `server-types.ts` could reference it). `server.ts` down to 12,117 lines (−989); no new tests needed — every path this pass moved is already covered by existing tests exercising the HTTP/route surface and `/messages/process`, not server.ts-private functions. All 224 tests pass unchanged; Agent Runtime v3 has no next-week-planning tool of its own and is unaffected. Phase 14 was an audit-only pass on `/messages/process` itself — the largest remaining monolithic surface in `server.ts` after phase 13 — to determine whether its ~252-line handler body (`server.ts:412-664`) and shared dispatcher cluster could finally move into `apps/api/src/legacy/messages-process.ts`. Verdict: **not safe yet (Classification C), no code moved.** Every dispatcher in the handler's call graph is either too broad to isolate (`handleSemanticRouterIntent`/`detectDeterministicSemanticRouterIntent`/`detectConversationSurfaceIntent` span 18+ product domains — onboarding, attention, review, planning, goals, actions, memory, email, Gmail, integrations, daily loop — none of it messages-process-specific) or blocked by the exact same dependency that blocked phase 13: `buildWeeklyReviewContext`, weekly review's own ~500-line, never-extracted context builder. This audit found that blocker is transitive into two more places phase 13 didn't touch — `resolvePendingDecisionReply` (the pending-decision resolver, called only from `/messages/process`) calls `resolveNextWeekPlanReply` directly, and `handleConversationSurfaceIntent` (also called only from `/messages/process`) calls `buildWeeklyReviewContext` directly for its `weekly_review` intent and `createPlanForConversation` for its planning intents — both inherit the block. No tiny safe extraction surfaced: the handler's own non-dispatcher helpers (`createGuardianGuardrailReply`, `maybeCreateManualActionFromText`, `composeFinalAgentResponse` at line 7457, `maybeAnalyzeWithOpenAI` at line 10442) are each shared with other call sites and scattered thousands of lines apart, so extracting the handler shell alone would just relocate glue code that still needs a dozen imports back from `server.ts`. `server.ts` unchanged at 12,117 lines. The audit's concrete conclusion: the actual prerequisite for ever extracting `/messages/process` is a dedicated pass on weekly review's own cluster (`buildWeeklyReviewContext` and ~20 sibling functions, `server.ts:3110-3591`), mapped and extracted the same way the Gmail/hygiene/planning passes handled their domains. Phase 15 did exactly that: mapped and extracted the full weekly-review context/analysis/formatting cluster — 31 functions/types across three non-contiguous locations in `server.ts` (split apart by phase 13's planning extraction landing in the middle of what was originally one block) — into `apps/api/src/legacy/weekly-review-conversation.ts`. Unlike every prior pass in this series, no hard blocker turned up: everything in the cluster operates on an already-built context or builds one from plain DB reads plus one already-extracted dependency (`analyzeActionHygiene`). Six generic helpers were shared with domains outside weekly review and got the same de-duplication treatment as every phase before — `startOfLocalWeek`/`isDateInRange` → `utils/datetime.ts` (needed by `buildNextWeekPlanContext`, which stays in server.ts), `containsUnsafeReflectionLanguage` → `utils/text.ts`, and two brand-new tiny files, `utils/events.ts` (`isGuardrailEvent`) and `utils/memory.ts` (`isOperatorReflectionMemory`), both shared with the operator-reflection feature that stays in server.ts, plus `utils/email-review.ts` (`EmailReviewKind` type + `emailReviewKind` function, leaving `emailReviewKindSortIndex`/`emailReviewGroupLabel`/`normalizeEmailReviewKind`/`emailReviewKindFromTarget` behind in server.ts since weekly review doesn't need them). The three weekly-review HTTP routes (`POST/GET .../weekly-review`, `GET .../weekly-review/last`, `GET .../weekly-review/context` — backing several Telegram slash commands, confirmed via `apps/telegram-bot/src/index.ts:1602-2779`) stayed registered in `server.ts` unchanged, now importing from the new module. This directly fixes the known blocker: `buildNextWeekPlanContext` and `resolvePendingDecisionReply` (both still in server.ts) now import `buildWeeklyReviewContext` one-directionally instead of calling a local, unexported function. `server.ts` down to 11,415 lines (−702); no new tests needed — every path this pass moved is already covered by existing tests exercising the three weekly-review routes, next-week planning, and `/messages/process`'s weekly-review/planning phrasing. All 224 tests pass unchanged; Agent Runtime v3 has no weekly-review tool of its own and is unaffected. Resolving this blocker does not by itself flip phase 14's Classification-C verdict for `/messages/process` as a whole — `handleSemanticRouterIntent`/`detectDeterministicSemanticRouterIntent`/`detectConversationSurfaceIntent` are still too broad to isolate, and `handleConversationSurfaceIntent`/`resolvePendingDecisionReply` still have their own remaining non-weekly-review breadth — so a fresh, full re-audit against the current dependency graph is the recommended next step before attempting `/messages/process` extraction again. Phase 16 was that re-audit. The handler body itself (`server.ts:430-682`) was unchanged since phase 14 — none of the four extractions between the two audits touched it, only functions it calls. Re-checking each dispatcher confirmed `handleConversationSurfaceIntent` and `resolvePendingDecisionReply` now resolve `buildWeeklyReviewContext` cleanly, but neither became fully safe: `handleConversationSurfaceIntent` still calls `buildOnboardingState`/`buildOperatorAttentionState`/`generateDailyOperatorBrief`/`buildStartDayMessage`/`buildConversationDailyReview`/`handleNaturalDailyLoopSettings` — all un-extracted onboarding/operator-attention/daily-brief domains unrelated to weekly review — and `resolvePendingDecisionReply` has a newly-clear blocker that was previously masked by the weekly-review one: `resolveEmailReviewContextReply` (`server.ts:8180`, ~119 lines), the un-extracted email-review domain, also called independently from `handleSemanticRouterIntent`. `handleSemanticRouterIntent`/`detectDeterministicSemanticRouterIntent`/`detectConversationSurfaceIntent` remain unchanged and too broad (18+ domains) on their own terms. Verdict: **Classification B** — re-reading `resolvePendingDecisionReply`'s remaining local dependencies fresh surfaced one genuinely clean piece, `resolvePendingCustomEmailRuleReply` (the "which Gmail rule do you mean" reply resolver), every dependency of which was already an extracted module or a plain DB call. It moved into the existing `apps/api/src/legacy/gmail-conversation.ts` (its natural domain home) rather than a new file. A side effect: with it gone, server.ts's entire `conversation/email-rule-selection.ts` import block (8 names) became fully unused and was deleted — that machinery was only ever needed by the already-extracted Gmail conversation cluster and this one function, confirming the boundary was correctly drawn in every prior Gmail-related pass. `server.ts` down to 11,339 lines (−76); no new tests needed — the moved function's only reachability path is unchanged in behavior and already covered by existing HTTP-level tests. All 224 tests pass unchanged. Phase 17 closed the second named blocker from phase 16: the email-review domain. ~56 "EmailReview"/"emailAttention"-named functions/types/routes were traced by dependency, not just by name, into four groups — the operator-attention email summary (feeds `buildOperatorAttentionState`, a different still-open blocker, left untouched), Gmail-sync-coupled review creation (`createEmailReviewItemForClassification` and helpers, called only from `syncEmailSignalRule` with a raw Gmail API message and access token, explicitly out of scope, left untouched), the pure email-review service (inbox listing/formatting, approve/reject, action conversion), and the legacy NL/pending-decision conversation layer built on top of it (`resolveEmailReviewContextReply` and its cluster — the actual blocker). Groups 3 and 4 were interleaved at a fine grain in the same file region, requiring per-function classification rather than one contiguous cut. 55 functions/types moved into two new modules, mirroring the `gmail-rule-service.ts` + `legacy/gmail-conversation.ts` precedent from the very first pass in this series: `apps/api/src/email-reviews/email-review-service.ts` (32 functions/types, the pure service layer) and `apps/api/src/legacy/email-review-conversation.ts` (23 functions, the legacy conversation layer, importing the service module one-directionally). Two more generic helpers were de-duplicated the same way as every phase before: `truncatePlainText`/`safeErrorForLog` → `utils/text.ts`, and a new `utils/action-goal-link.ts` (`inferActionGoalLink`, shared with manual action creation which stays in server.ts). The five email-review HTTP routes stayed registered in `server.ts` unchanged, now importing from the service module. This directly fixes the blocker phase 16 identified: `resolvePendingDecisionReply`'s `email_review_context` branch now imports `resolveEmailReviewContextReply` one-directionally instead of calling a local function. `server.ts` down to 10,229 lines (−1,110, the largest single-pass reduction since the legacy Gmail conversation cluster extraction); no new tests needed — every path this pass moved is already covered by existing tests exercising the five email-review routes and `/messages/process`'s email-review phrasing. All 224 tests pass unchanged; Agent Runtime v3 has no email-review tool of its own and is unaffected. `resolvePendingDecisionReply`'s only remaining server.ts-local dependency is now `resolveNextWeekPlanReply` — a separate, already-understood blocker from the planning pass, unchanged by this one — and `handleConversationSurfaceIntent`'s operator-attention/onboarding/daily-brief dependency (phase 16's other named blocker) remained open at the time, so a fresh, third `/messages/process` audit was recommended once that was addressed. Phase 18 closed that remaining blocker: the operator-attention/onboarding/daily-brief cluster. Traced by dependency across four non-contiguous regions of `server.ts` — the attention/brief builders (`buildOperatorAttentionState`, `generateDailyOperatorBrief`, `buildStartDayMessage`, `buildEndDayMessage`, `buildTomorrowPrepMessage`) and the daily-loop HTTP routes' backing implementation; a daily-brief-context/coach sub-cluster (`buildDailyBriefContext`, `maybeGenerateDailyCoach`, and 5 daily-coach-error helpers) found only by tracing `generateDailyOperatorBrief`'s body in full, sitting between the operator-reflection domain and `handleConversationSurfaceIntent`; the onboarding/setup + daily-review + daily-loop-settings conversation cluster, interleaved at a fine grain with planning/show-goals/integration-guidance code that stays in server.ts; and a 30-function attention-formatting/support cluster sitting immediately before three unrelated functions (`findGoalBySelector`, `formatLocalDate`, `formatLocalActionDueKey`) that per-function caller-scope verification confirmed belong to other domains and stayed put. One blocker surfaced mid-mapping, the same shape as the planning/weekly-review blockers earlier in this series: `generateDailyOperatorBrief` calls three operator-reflection functions. Two (`selectRelevantOperatorReflection`, `formatOperatorReflectionForBrief`) turned out to be exclusively used by it, so they moved cleanly; the third (`getActiveOperatorReflections`) is genuinely shared with operator reflection (which stays in server.ts), so it was de-duplicated into `apps/api/src/utils/memory.ts` alongside the already-there `isOperatorReflectionMemory`, following that exact precedent — no part of operator reflection itself needed to move. 47 functions/types moved into two new, independent-sibling modules (confirmed via full cross-call check in both directions — neither imports the other): `apps/api/src/operator/attention.ts` (the attention/brief-builder and formatting/support groups, 1,370 lines) and `apps/api/src/legacy/daily-conversation.ts` (the onboarding/daily-review/daily-loop-settings conversation group, 401 lines). One more generic helper was de-duplicated the same way as every phase before: `sortGoalsForDisplay` → a second export on `apps/api/src/utils/goal-priority.ts`. The daily-loop HTTP routes, the onboarding HTTP routes, and the reflections route stayed registered in `server.ts` unchanged, now importing from the new modules; `handleConversationSurfaceIntent`, `handleSemanticRouterIntent`, and `executeMultiIntentPlan` (the shared dispatchers that stay in server.ts) now import every one of these handlers instead of calling local functions. 24 now-orphaned imports left over from the moved code were also removed, confirmed via `git diff` to be newly orphaned by this pass rather than pre-existing dead imports from an earlier one (several genuinely pre-existing unused imports were found and deliberately left untouched). `server.ts` down to 8,538 lines (−1,691, the largest single-pass reduction in this whole cleanup series); no new tests needed — every path this pass moved is already covered by existing tests exercising the daily-loop HTTP routes, onboarding natural-language surfaces, and `/messages/process`'s daily-review phrasing. All 224 tests pass unchanged; Agent Runtime v3 has its own independent daily-brief/attention tool implementation and is unaffected. This was the second and last of the two named blockers from phase 16's audit — `resolvePendingDecisionReply`'s only remaining local dependency is `resolveNextWeekPlanReply` (planning's own separate blocker), and `handleConversationSurfaceIntent` is now a thin dispatcher over extracted modules, though it and the semantic/deterministic router functions still span 18+ domains by inherent design rather than by missing-module blocker. A fresh, third `/messages/process` extraction readiness audit is the recommended next step, to determine whether that remaining breadth changes the Classification-C verdict or whether the honest conclusion is that these dispatchers are appropriately broad shared infrastructure. Phase 19 was that audit, and it flipped the verdict. Re-reading `detectDeterministicSemanticRouterIntent` and `detectConversationSurfaceIntent` fresh (rather than trusting the "18+ domain breadth" characterization carried forward from phase 14) found that five extraction passes since then had each already pulled their own domain's logic out from under these two pattern-matchers — what remained was almost entirely delegation to already-extracted-module predicates, not inline domain breadth. One real blocker turned up: `createPlanForConversation` calls `buildNextWeekPlanContext`, which stayed in server.ts not because of any remaining weekly-review entanglement (already resolved) but because the original planning pass had left it behind, and it's also needed by three dedicated `next-week-plan` HTTP routes. Since `apps/api/src/legacy/planning-conversation.ts` already existed as a clean sibling of the weekly-review module, `buildNextWeekPlanContext` and `resolveNextWeekPlanReply` moved there as a small prerequisite step — the same "prerequisite extraction closes the loop" pattern that resolved every blocker in this series. A second, narrower issue — `handleConversationSurfaceIntent`/`handleSemanticRouterIntent`'s `gmail_sync`/`integration_sync` branches calling server.ts's own Gmail OAuth/sync orchestrator, which must stay in server.ts — was resolved without touching any Gmail/OAuth code: `apps/api/src/legacy/messages-process.ts` exports `createMessagesProcessHandler(deps)`, a factory taking `syncGmailForConversation`/`syncIntegrationsForConversation` as constructor dependencies, mirroring the `defaultAgentRouteHandlers()` factory pattern already used for `registerAgentRoutes`; server.ts wires the concrete sync functions in at route-registration time. Three more small shared-helper clusters were de-duplicated the same way as every phase before, each split between the moving handler and a sibling that stays in server.ts: `apps/api/src/actions/manual-action.ts` (new, `maybeCreateManualActionFromText` and its cluster, shared with the `/actions/manual` route and multi-intent execution), `apps/api/src/conversation/final-response.ts` (new, `composeFinalAgentResponse`/`buildAgentContext`/`withMemoryContextReply`, shared with the multi-intent guardrail executor), and `apps/api/src/utils/pending-memory.ts` (new, the repeated-cooldown/low-sleep pending-memory checks, shared with the check-in ingestion routes). `apps/api/src/utils/datetime.ts` gained `tomorrow` and `parseOptionalNow` (the latter previously server.ts's single most widely-shared "now" parser, ~26 call sites). Verdict: **Classification A** — everything genuinely exclusive to `/messages/process`'s call graph (~90 functions/types: the handler body, both dispatchers, structural-proposal/manual-action detection, pending-decision resolution) moved into `apps/api/src/legacy/messages-process.ts` (2,630 lines), nothing left half-extracted. A large pass of now-orphaned imports (~90 named imports) was also removed from server.ts's top-level import block, confirmed via `git diff` per-symbol to be newly orphaned by this pass rather than pre-existing (a handful of genuinely pre-existing unused imports were found and deliberately left untouched, same as every phase before). `server.ts` down to 5,528 lines (−3,010, the largest single-pass reduction in this entire series, surpassing phase 11's −1,997). No new tests needed — every path this pass moved is already covered by `tests/messages-process-legacy.test.ts` and every test exercising pending-decision confirmation, deterministic conversation surfaces, and the legacy semantic router. All 224 tests pass unchanged; Agent Runtime v3 never called any of this legacy code and is unaffected. Next cleanup target: `handleConversationControl`/`executeMultiIntentPlan`/`executeGuardrailMessage`, the dispatcher cluster behind the separate `/conversation/control` and `/conversation/multi-intent` HTTP routes — the next-largest monolithic surface in `server.ts`, and possibly similarly over-estimated in remaining breadth now that its likely shared dependencies already have clean homes from this pass. See `docs/09-architecture-inventory.md`'s "Server.ts Map", "Conversation Orchestrator v2 — Full Retirement", "LLM Operation-Planner Package Cleanup", "Core Conversation-Orchestrator Types Cleanup", "Gmail Rule Service Extraction", "Action Hygiene Service Extraction", "Pending-Actions Route Extraction", "Legacy Gmail Conversation Cluster Extraction", "Action-Hygiene Legacy Conversation Cluster Extraction", "Planning Legacy Conversation Cluster Extraction", "`/messages/process` Extraction Readiness Audit", "Weekly-Review Legacy Cluster Extraction", "`/messages/process` Extraction Readiness Audit — Second Pass", "Email-Review Domain Extraction", "Operator-Attention/Onboarding/Daily-Brief Cluster Extraction", and "`/messages/process` Third Extraction Readiness Audit" for the full dependency map, dispatcher-by-dispatcher risk table, and current cleanup targets. **This 20-phase `server.ts` line-count-reduction series is now paused as the project's primary goal** (`server.ts` sits at 5,528 lines, down from 17,903) — see the next bullet and `docs/09-architecture-inventory.md`'s "Strategy Change — Capability Migration Into Agent Runtime v3" for what replaces it.
- [x] **Agent Runtime v3 capability migration — action hygiene.** First pass of the new capability-migration strategy: "clean up my actions" (and its numbered multi-decision replies — "complete 1, snooze 2 to Friday, archive 3", "keep 1 and archive 2") is now handled entirely by Agent Runtime v3's own typed tools, `action.hygiene_start`/`action.hygiene_apply`, with zero call into legacy `/messages/process`. The candidate-detection logic (`analyzeActionHygiene` and formatting siblings) was split out of `apps/api/src/legacy/action-hygiene-conversation.ts` into a new non-legacy module, `apps/api/src/actions/hygiene-session.ts`, so both the legacy conversation module and v3 import the same code instead of v3 depending on `legacy/*`; the batch-mutation logic (`applyActionHygieneBatchOperations` in `apps/api/src/actions/hygiene.ts`, already non-legacy) is reused as-is. Numbered-list resolution ("complete 1") is deterministic: `action.hygiene_start` tags each shown action with its list position via a new `AgentEntity.index` field, and `action.hygiene_apply`'s validator resolves each decision's real action id against the session's stored visible entities — never against hidden LLM memory — asking for clarification instead of guessing when no hygiene list is currently visible. Legacy `resolveActionHygieneReply` (free-text NL parser) and the `GET .../actions/hygiene`/`POST .../actions/hygiene/reply` routes are unchanged and still back Telegram's `/action_hygiene` and `/debug_action_hygiene` slash commands — not deleted, since slash commands still reach them directly. 11 new tests in `tests/agent-runtime-action-hygiene.test.ts`; full suite 235/235 passing. See `docs/09-architecture-inventory.md`'s "Action Hygiene Migrated Into Agent Runtime v3" for the full capability map, files changed, and next migration candidate (next-week planning or weekly review).
- [x] **Agent Runtime v3 capability migration — next-week planning.** Second pass of the capability-migration strategy: "plan next week" (and "help me plan next week", "make a plan for next week based on my goals") is now handled entirely by Agent Runtime v3's own typed tools, `planning.next_week_start`/`planning.next_week_edit`/`planning.next_week_apply`, with zero call into legacy `/messages/process`. The plan-context/suggestion-generation logic (`buildNextWeekPlanContext`, `generateNextWeekPlanSuggestions`, and their pure formatting/dedup siblings) was split out of `apps/api/src/legacy/planning-conversation.ts` into a new non-legacy module, `apps/api/src/planning/next-week.ts`, so both the legacy conversation module and v3 import the same code instead of v3 depending on `legacy/*`; the DB-mutation path (`actionInputFromPlanSuggestion` + `createActionItemIfNotExists`, already non-legacy) is reused as-is. The flow is a full propose → adjust → confirm loop, not a single-shot tool: `planning.next_week_start` shows a numbered draft and opens a pending confirmation on its own (via a new generic `ExecutedOperation.pendingOperationUpdate` mechanism, since the tool must actually execute to show real data, unlike `gmail.rule.create`'s static confirmation prompt); `planning.next_week_edit` applies "remove 2"/"change 1 to Tuesday"/"make it lighter" to the open draft and re-shows it, deliberately modeled as non-mutating so it's never blocked by the pending-operation firewall; an exact "yes" (now including "looks good") applies the draft and creates the action items, reusing the same dedup-against-existing-open-actions logic the legacy create flow already used. `planning.next_week_apply` (the actual DB-mutation tool) can never be planned by the LLM directly — the validator rejects it outright, so the only way it ever runs is via the deterministic confirm whitelist re-executing an already-stored pending operation. Legacy `resolveNextWeekPlanReply`/`parseNextWeekPlanReply` (the opt-in-create free-text parser: "create 1", "create all new", "edit 2 to Friday morning") and the `POST .../next-week-plan` route are unchanged and still back Telegram's `/plan_next_week` slash command — not deleted, since the slash command still reaches them directly. 10 new tests in `tests/agent-runtime-next-week-planning.test.ts`; full suite 245/245 passing. See `docs/09-architecture-inventory.md`'s "Next-Week Planning Migrated Into Agent Runtime v3" for the full capability map, files changed, and next migration candidate (weekly review).
- [x] **Next-week planning hotfix — deterministic edits and natural-language UX.** A real Telegram transcript showed "remove 1" failing validation (`needs: removeIndexes.0`) while the *next* turn's draft showed item 1 removed anyway — a session mutation despite a reported validation failure. Root cause: the planner's JSON-schema generator (`apps/api/src/agent-runtime/tool-catalog.ts`) never propagated zod's `.describe()` text to the LLM (so "1-based" guidance never reached it) and never marked optional array fields nullable (the first optional top-level array fields in the tool catalog were `planning.next_week_edit`'s own `removeIndexes`/`changes`, so this bug had never been exercised before); separately, the old executor resolved indexes/dates per-item instead of atomically, so a request combining a stale index with a new change could partially apply. Fixed on both fronts: the schema generator now propagates descriptions and nullable-array typing for every tool (not just planning's); `planning.next_week_edit` resolution (numeric indexes, natural refs, day-text parsing) now happens entirely in the validator before anything executes — either the whole edit resolves against the current draft, or nothing runs and `pendingOperation`/`visibleEntities` are left byte-for-byte untouched. Alongside the fix, `planning.next_week_edit` gained natural-language item references — `removeRefs`, `changes[].ref`, `keepIndexes`/`keepRefs` ("keep X and remove the rest"), and a deterministic `lighter` flag for "make it lighter" (code decides what's droppable by priority; never LLM-guessed) — resolved against the current draft's own title/goal-title text via fuzzy token matching (`apps/api/src/planning/next-week.ts`'s `resolvePlanSuggestionRef`/`computeLighterPlanRemoval`), with numbered indexes kept as a fully-working fallback rather than the primary suggested UX. The bot's own copy changed to match: "Here's a draft plan for next week plan:" (a title/phrase duplication bug) → "Here's a draft plan for next week:"; "Reply: yes, remove <n>, change <n> to <day>, or cancel." → "Tell me naturally what to change — for example: 'move the gym one to Friday', 'remove the YouTube item', 'make it lighter', or 'yes' to create it." 13 new tests; full suite 258/258 passing. See `docs/09-architecture-inventory.md`'s "Next-Week Planning Hotfix — Deterministic Edits and Natural-Language UX" for the full root-cause writeup and resolution-order rules.
- [x] **Next-week planning V3 state-machine hardening.** Real Telegram smoke testing (not unit tests, which all passed) found the hotfix above still had multi-turn state bugs: a generic "Agent v3 hit an error" on some first-ever "plan next week" turns (a `Promise.all` race between `ensureUser` and other foreign-key-dependent queries in `loadContext`, fixed by awaiting `ensureUser` first, plus a new top-level safety net in `runtime.ts` so any unexpected error now replies gracefully instead of crashing); "move the jobs to Friday" on a single-item draft emptying the plan, and then — after a first fix pass — getting rejected outright even for a plain move, because the real planner keeps sending a redundant remove alongside the change for the item it's moving (fixed with fully atomic edit resolution where an explicit change to an item always wins over a redundant remove of that same item, checked before the "would this empty the plan" guard); "show me the week plan" regenerating a brand-new draft instead of showing the current one (fixed with a new `planning.next_week_show_current` read-only tool); and a rejected edit's reply still containing the planner's own pre-execution `replyDraft` falsely claiming success (fixed by never blending `replyDraft` into a clarification reply — a real, general `response-composer.ts` bug, not planning-specific). Also added a dev/test-only `AGENT_RUNTIME_PLANNING_TRACE=true` diagnostic (`debug.planningTrace`) capturing a planning turn's full pipeline — never active in production. 12 new tests across two fix passes; full suite 270/270 passing. See `docs/09-architecture-inventory.md`'s "Next-Week Planning V3 State-Machine Hardening" for the full root-cause writeup.
- [x] **V3 scripted conversation smoke/eval harness.** A lightweight (not a general eval framework) way to replay the exact multi-turn conversation shapes that found the bugs above automatically: `tests/helpers/agent-runtime-test-helpers.ts` (generic mock/send/session primitives, extracted out of the near-duplicate copies in `agent-message.test.ts`/`agent-runtime-action-hygiene.test.ts`/`agent-runtime-next-week-planning.test.ts`, which were left otherwise untouched) and `tests/helpers/agent-runtime-scripted-eval.ts` (`runScriptedScenario` — runs a fixed `{message, plan?, assert?}` turn sequence through the same `POST /agent/message` route Telegram uses, capturing `pendingOperation`/`visibleEntities` before/after every turn, plus reusable assertions like `assertNoGenericError`/`assertNoFalseSuccessClaim`/`assertPlanningDraftInSync`). `tests/agent-runtime-scripted-smoke.test.ts` ships the 5 required scenarios (planning happy path replaying the real reported transcript end to end, planning cancel path, no-stale-replyDraft, action-hygiene smoke, memory/Gmail/today smoke) as plain TypeScript object literals — a scenario's turn data is JSON-serializable, so it could equally live in a JSON fixture. Every shipped scenario mocks the planner explicitly (deterministic, CI-safe, no `OPENAI_API_KEY` needed); omitting a turn's `plan` field lets the real planner run instead, for an optional local run against the live model — the only way to actually catch a live-planner behavior gap like the "move the jobs to Friday" issue above, since a mocked test can only prove the code handles a given input correctly, not that the LLM actually sends that input. Zero production files changed — test-only. 5 new tests; full suite 275/275 passing. See `docs/09-architecture-inventory.md`'s "V3 Scripted Conversation Smoke/Eval Harness" for how to add a new scenario.
- [x] **Agent Runtime v3 capability migration — weekly review.** Third pass of the capability-migration strategy: "review my week" (and "give me my weekly review", "how did this week go?", "what changed this week?" — week-scoped, not confused with bare "what changed?"/operator.recent_changes — "what should I improve next week?") is now handled entirely by Agent Runtime v3's own typed tools, `weekly_review.start`/`weekly_review.save`, with zero call into legacy `/messages/process`. `apps/api/src/legacy/weekly-review-conversation.ts` (734 lines) turned out to have zero legacy/PendingAction coupling anywhere, so effectively its entire content moved verbatim into two new non-legacy modules, `apps/api/src/weekly-review/context.ts` (context building) and `apps/api/src/weekly-review/review.ts` (generation/saving/formatting) — the legacy file is now a two-line re-export. Saving already existed (`generateAndSaveWeeklyReview`, reused as-is, zero duplicated DB logic) but legacy always auto-saves on every generate; V3 deliberately splits "show" from "save" into two explicit steps, matching the propose→confirm idiom next-week planning already established (a minimal, task-sanctioned divergence, not a broad rewrite). `weekly_review.start` shows a grounded, deterministically-generated review (or a distinctly shorter, honest "not much was logged this week" reply for a genuinely thin week) and opens a pending save state; like `planning.next_week_apply`, `weekly_review.save` can never be planned by the LLM directly (validator rejects it outright) — it only runs via the deterministic confirm whitelist, now also matching the explicit phrases "save this review"/"save the review"/"save it" alongside the existing "yes"/"looks good". 9 new tests in `tests/agent-runtime-weekly-review.test.ts`, plus a 6th scripted scenario in `tests/agent-runtime-scripted-smoke.test.ts` replaying "review my week → what should I improve next week? → save this review → so what today" end to end; full suite 285/285 passing. See `docs/09-architecture-inventory.md`'s "Weekly Review V3 Migration" for the full capability map, files changed, and next migration candidate.
- [x] **Agent Runtime v3 capability migration — Gmail rule management.** Fourth pass of the capability-migration strategy: pausing/resuming/removing an existing Gmail tracking rule ("turn off Endesa", "pause the Naturgy rule", "delete the Aigues rule", "resume X") is now handled entirely by Agent Runtime v3's own typed tools, `gmail.rule.propose_update`/`gmail.rule.apply_update`, with zero call into legacy `/messages/process`. Unlike the three prior passes, there was no large legacy cluster to extract — the reusable core (`apps/api/src/conversation/email-rule-selection.ts`'s target-name matching, `apps/api/src/gmail/gmail-rule-service.ts`'s rule listing, and the plain `@operator-agent/db` mutation functions) was already non-legacy from an earlier cleanup pass; a new, small `apps/api/src/gmail/gmail-rule-management.ts` holds only the genuinely new pure helpers (supported-operation labels, already-in-state checks, proposal phrasing). `gmail.rule.propose_update` resolves the target rule by name against a **fresh** DB lookup every time (not stored session state), so it works whether or not the user listed rules first; ambiguous or unknown refs get a grounded, non-mutating reply and leave any pending state untouched (necessarily resolved in the executor, not the validator, since target resolution needs an async DB read the synchronous validator can't do — a `mutates: false` op with no `pendingOperationUpdate` set is exactly as inert as a validator-level rejection). Deliberately requires confirmation for all three operations (pause/resume/archive) — a minor, task-specified safety tightening versus legacy's immediate-pause behavior, matching `gmail.rule.create`'s existing confirm-required precedent; `gmail.rule.apply_update` can never be planned by the LLM directly, same pattern as `planning.next_week_apply`/`weekly_review.save`. Explicitly does not support changing an existing rule's review-only/auto-log behavior — confirmed there is no existing path (conversational or HTTP) that does this today, so the planner is told to explain that honestly rather than a new toggle capability being invented. `apps/api/src/legacy/gmail-conversation.ts` was not touched at all. 11 new tests in `tests/agent-runtime-gmail-rule-management.test.ts`, plus a 7th scripted scenario in `tests/agent-runtime-scripted-smoke.test.ts` replaying "what email rules are active? → turn off Endesa → yes → what email rules are active?" end to end; full suite 297/297 passing. See `docs/09-architecture-inventory.md`'s "Gmail Rule Management V3 Migration" for the full capability map, files changed, and next migration candidate.
- [x] **Agent Runtime v3 capability migration — daily-loop settings.** Fifth pass of the capability-migration strategy: showing and changing daily-loop settings ("what are my daily loop settings?", "turn off daily review", "set my daily review to mornings", "turn daily check-ins back on") is now handled entirely by Agent Runtime v3's own typed tools, `daily_loop.settings_show`/`daily_loop.settings_propose_update`/`daily_loop.settings_apply_update`, with zero call into legacy `/messages/process`. Legacy's `handleNaturalDailyLoopSettings` (`legacy/daily-conversation.ts`) mutates `NotificationSettings` immediately with no confirmation and has no "off" support at all (hardcodes `dailyLoopEnabled: true`); the ability to disable the daily loop already exists in the system today via a different entry point (the Telegram bot's `/set_daily_loop enabled off` slash command, same underlying `updateNotificationSettings` call), confirming this is parity work, not new-behavior invention. Its two pure time helpers (`parseNaturalTimeToMinutes`/`formatMinutesOfDay`) were split out byte-identical into a new non-legacy module, `apps/api/src/operator/daily-loop-settings.ts`, which also adds a new `parseTimeOfDayText` for v3's planner (which already extracts a clean time phrase itself, unlike legacy's whole-message regex parser). Scope is deliberately bounded to the same three fields legacy's own handler covers — `dailyLoopEnabled`/`morningTimeMinutes`/`eveningTimeMinutes` — nothing else (no delivery channel, no other reminder types); the planner declines out-of-scope requests honestly instead of a new "unsupported" code path. `daily_loop.settings_propose_update` resolves and compares against real current settings and opens a pending confirmation via the same generic `ExecutedOperation.pendingOperationUpdate` mechanism planning/weekly-review/Gmail-rule-management use; deliberately requires confirmation for every mutation — a minor safety tightening versus legacy's immediate-mutation behavior, matching the precedent already set by weekly review and Gmail rule management. `daily_loop.settings_apply_update` can never be planned by the LLM directly, same pattern as `planning.next_week_apply`/`weekly_review.save`/`gmail.rule.apply_update`. `apps/api/src/legacy/daily-conversation.ts` keeps its own mutation/confirmation behavior untouched, now importing the two split-out time helpers instead of defining them locally. 10 new tests in `tests/agent-runtime-daily-loop-settings.test.ts`, plus an 8th scripted scenario in `tests/agent-runtime-scripted-smoke.test.ts` replaying "what are my daily loop settings? → turn off daily review → cancel → turn off daily review → yes → what are my daily loop settings?" end to end; full suite 308/308 passing. See `docs/09-architecture-inventory.md`'s "Daily-Loop Settings V3 Migration" for the full capability map, files changed, and next migration candidate.
- [x] **Agent Runtime v3 global readiness audit.** Audit + scripted-smoke expansion pass ahead of proactive-operator work — no product code changed, no legacy code deleted. Built a full capability matrix across every migrated (and unmigrated) V3 capability (`docs/10-v3-readiness-audit.md`), documented exact Telegram/`/messages/process`/slash-command routing reality with file:line citations, and expanded `tests/agent-runtime-scripted-smoke.test.ts` from 8 to 13 scenarios (added memory recall, goals/progress, action CRUD, Gmail review/email-to-action, and guardrail coverage). Headline findings: `gmail.review.list`/`reject`/`to_action` are fully wired end-to-end but `planner.ts` has zero prompt guidance for them, so a real LLM turn can't realistically discover them; there is no `goal.*` tool at all, so goal-related chat is either silently logged as a memory or answered with a bare count via `operator.today`; and V3's guardrail (`checkPolicyGuardrail`) is entirely user-configured-trigger-driven with no hard-coded safety net, unlike legacy's separate, LLM-classified gambling/trading-risk refusal mechanism, which V3 does not share — a genuine safety regression for any risky message typed into normal V3 chat by a user who hasn't pre-configured a matching trigger. No legacy module was found to be fully orphaned; deletion candidates and their blockers are itemized in the audit doc. Full suite: **313/313**. See `docs/10-v3-readiness-audit.md` for the complete capability matrix, routing findings, deletion-candidate analysis, and early-user readiness verdict (top blockers before external users, top improvements after baseline).
- [x] **Agent Runtime v3 generic goal/guardrail engine.** Closes the readiness audit's headline gambling/finance guardrail gap in a product-aligned way, explicitly *not* by porting legacy's hardcoded `betting_intent`/`trading_intent` classifier. New module `apps/api/src/agent-runtime/goal-guardrails.ts` replaces `validator.ts`'s old `checkPolicyGuardrail` with a two-tier, domain-agnostic engine: Tier 1 keeps the exact prior deterministic `knownTriggers`/`knownFailureModes` substring match; Tier 2 (LLM, only reached when Tier 1 finds nothing and the user has an active goal) classifies the message against the user's own goals only — never inventing one — into `none`/`soft_warn`/`hard_block`/`ask_clarification` with a pattern (`active_violation`/`avoidance`/`lapse_admission`). "Stop gambling," "Improve financial discipline," "Find a new developer job," and "Train 3x/week" are all just plain user-authored `Goal` rows to this code — nothing branches on domain. Mocked via `AGENT_RUNTIME_GUARDRAIL_MOCK_RESPONSE` (mirrors the planner's own mock pattern) so CI has zero `OPENAI_API_KEY` dependency; with neither a mock nor a real key, Tier 2 is skipped entirely and the turn defaults to allow, which is why every pre-existing test that happens to seed goals was completely unaffected. Runs before the planner in `runtime.ts`, same as the guardrail it replaces; on `hard_block`/`soft_warn` it logs a `risk_pattern` memory via the existing `createMemory` mechanism (already read by insights/daily-review) instead of inventing new schema — the LLM only classifies, deterministic code decides what to log, so the LLM never mutates the DB. Reply text is templated from the matched goal's title + pattern + the user's own `directness` profile field, never raw LLM prose. A returned `goalId` is validated against the actual goals passed to the classifier — an invented/mismatched id collapses back to allow rather than blocking on a goal that doesn't exist. 10 new tests in `tests/agent-runtime-goal-guardrails.test.ts` (covering cessation-goal violation, financial-discipline-as-goal not hardcoded finance, job-search avoidance, training-goal lapse support, no-relevant-goal passthrough, trigger-only path, recovery/support language correctly let through, invented-goalId rejection, ask_clarification), 2 rewritten/added scripted-smoke scenarios (`tests/agent-runtime-scripted-smoke.test.ts`, scenarios 13-15), and one pre-existing test in `tests/agent-message.test.ts` updated in place since it asserted the *old* "no mutation" guardrail behavior that the new risk-logging intentionally changes. Full suite: **325/325**. See `docs/10-v3-readiness-audit.md`'s §9 for the full design, examples, and the remaining intentional gap (no goal/trigger set → no intervention, by explicit product design).
- [x] **Agent Runtime v3 Gmail review triage reachable via normal chat.** Closes the readiness audit's other originally-partial gap: `gmail.review.list`/`reject`/`to_action` were already fully wired mechanically but unreachable by a real LLM turn (zero `planner.ts` guidance, bare-count summary with no way to learn a review's real id). No Gmail OAuth/callback/sync/provider code touched; no send/reply/label capability added. `planner.ts` gained explicit guidance for all three tools, covering "what emails need my attention?"/"what Gmail reviews are waiting?"/"show pending email reviews"/"anything important in Gmail?" (list), "turn the recruiter one into a task"/"make the recruiter email an action" (to_action), and "reject/ignore the Endesa one" (reject) — both mutating tools reference the most recently shown list by `index` or `ref`, never inventing a `reviewId`. `gmail.review.list`'s summary changed from a bare count to an itemized, numbered list (new `formatGmailReviewListForChat`/`gmailReviewChatLabel`/`gmailReviewChatDescription` in `apps/api/src/email-reviews/email-review-service.ts`, reusing that module's existing `extractSafeSenderLabel`/`truncatePlainText` for PII-safety) with each item stored as an indexed `gmail_review` visible entity. New `resolveGmailReviewRef` in `validator.ts` resolves `index`/`ref` synchronously against `context.session.visibleEntities` — never a fresh DB lookup, never hidden LLM memory; explicit index always wins, free-text `ref` reuses the existing `selectEmailRuleCandidate` matcher (`apps/api/src/conversation/email-rule-selection.ts`, the same one legacy's numbered Gmail-rule flow already relies on) rather than a new fuzzy matcher; zero or multiple matches both ask clarification with no mutation. A real cross-turn visibility bug was found and fixed: `runtime.ts`'s `applyExecutionSideEffects` replaces the entire visible-entities list each turn, so both mutating tools now re-include the other still-pending reviews (re-numbered) in their own result, not just the one thing that turn changed. `gmail.review.to_action` now calls the existing `createActionItemFromEmailReview` service function (real title generation + goal-linking) instead of a bespoke inline construction; `gmail.review.reject` calls the existing `rejectEmailReviewForUser`. Both added to `response-composer.ts`'s `GROUND_TRUTH_ONLY_TOOLS` so replies always state exactly what was created/rejected. 11 new tests in `tests/agent-runtime-gmail-review-triage.test.ts`, plus scripted-smoke scenario 12 rewritten to the full triage flow (seed two reviews → list → convert one by ref → reject the other by ref → list again, reflecting real DB state, no legacy Gmail setup wall). Full suite: **336/336**. See `docs/10-v3-readiness-audit.md`'s §10 for the full design and the one known remaining limitation (ambiguous vs. unknown references share one generic clarification message).
- [x] **Agent Runtime v3 goal-listing honesty.** Closes the last pre-proactive-operator baseline gap: V3 had no `goal.*` tool, so "what are my goals?" either fell through to `operator.today`'s bare active-goal count or risked silently becoming a `memory.create` that let the user believe a real goal was now tracked. Deliberately minimal per this task's scope — **no goal creation/update/delete was added**, only read-only listing plus an honesty rule for creation requests. New tool `goal.list` (`apps/api/src/agent-runtime/tool-catalog.ts`, `mutates: false`, no args) reads `context.activeGoals` directly (already loaded every turn, zero extra DB calls) and formats it with a new `formatGoalListForChat` helper in `executor.ts` — numbered `title — category — priority`, an indented `Why: ...` line when set, always closing with an honest "goal editing isn't wired through chat yet, use /create_goal" boundary line, and an honest empty state when there are none. `planner.ts` gained two-part guidance: `goal.list` for "what are my goals?"/"show my active goals"/"what am I working on?"/"which goals are active?"/"why did I set the X goal?"; and an explicit rule that an actual creation/edit request ("create a goal to X", "update/delete my X goal") must plan zero operations with an honest decline pointing to `/create_goal` — never `memory.create` as a silent substitute. A narrower, genuinely-ambiguous statement of intent ("I want to find a developer job") can still become a `memory.create(type: "goal_context")` as before, but its `replyDraft` is now required to be explicit that this is only being remembered, never framed as a created/tracked goal. `createGoal` (`@operator-agent/db`) was confirmed already clean and well-typed during this pass's mapping step, but deliberately not wired up this pass, per the task's explicit preference for an honest decline over adding creation. `goal.list` is read-only, so it flows through `response-composer.ts`'s existing informational-tools branch unchanged — no composer changes needed. 7 new tests in `tests/agent-runtime-goal-listing.test.ts` (real titles returned, priority/category/why included, honest empty state, explicit-creation-request creates neither a Goal row nor a substitute memory, ambiguous-intent reply never claims a goal exists, progress logging and the goal-guardrail engine both confirmed unaffected), plus scripted-smoke scenario 10 rewritten to chain: two seeded goals → "what are my goals?" (titles appear) → "I sent 3 CVs today" (event logged) → "create a goal to run a marathon" (zero fake goal, zero fake memory). Full suite: **343/343**. See `docs/10-v3-readiness-audit.md`'s §11 for the full design and the two remaining open decisions (whether to eventually wire `createGoal` up, and the separate onboarding-guardrail-nudge question from §9).
- [x] **Agent Runtime v3 goal-anchor nudge for empty users.** Closes the last pre-proactive-operator product question: the goal-aligned guardrail engine has nothing to work with for a genuinely empty user (no active goals, no `knownTriggers`/`knownFailureModes`). Explicitly not full onboarding, not goal creation, and not proactive/scheduled push behavior. New `shouldShowGoalAnchorNudge` in `apps/api/src/agent-runtime/runtime.ts` runs right before the planner call — the same slot as the goal-guardrail check and the exact confirm/cancel whitelist — returning true only when the user has zero active goals, zero configured triggers/failure modes, no pending operation, and the trimmed message exactly matches `BROAD_OPERATOR_QUESTION_RE`, a deliberately narrow allowlist ("what should I do (today)?", "what can you help with?", "help me get organized/started", "so what today", "how do I start", "where do I start", "what now", "what's next") — never a specific-capability request, which always reaches the planner unchanged. No new schema: "was the nudge just shown" reuses the existing `session.messages` history (`wasGoalAnchorNudgeShownLast` checks the most recent assistant message for a marker phrase in the nudge's own text), giving a simple one-turn suppression rather than a persistent cooldown — the very next broad question after a nudge falls through to normal planning instead of repeating. Reply text closes with the same honest "/create_goal for now, or tell me the context and I'll remember it" boundary line `goal.list` already uses. Three pre-existing tests (`tests/agent-runtime-scripted-smoke.test.ts` scenarios 5 and 11, `tests/agent-message.test.ts`'s operator.today slash-command test) that sent "so what today"/"what should I do today?" from a zero-goal user were updated to seed one realistic goal, since that's now exactly the case the nudge is meant to intercept — the same "update the fixture, not the feature" pattern used for the pre-existing guardrail test in the goal-guardrail-engine pass. 7 new tests in `tests/agent-runtime-goal-anchor-nudge.test.ts` (nudge fires for two broad phrasings from an empty user; suppressed by an active goal; suppressed by a configured `knownTrigger` alone; suppressed for a specific capability request; `goal.list`'s own empty state wins over the nudge; no immediate repeat) plus scripted-smoke scenario 16 (empty user → nudge → immediate second broad question → no repeat → seed a goal → normal `operator.today` resumes). Full suite: **351/351**. See `docs/10-v3-readiness-audit.md`'s §12 — this clears the path to the **V3 Proactive Operator MVP**, the next recommended step.
- [x] **V3 Proactive Operator MVP — decision/preview layer.** First proactive layer for Agent Runtime v3, deliberately **not** wired to actual scheduled sending yet — a pure decision module plus a preview-only route, per this task's "prefer a callable decision module first" / "do not send real messages automatically" rules. `apps/worker` already runs a full legacy proactive system (daily-loop start-day/end-day, check-ins, insights, action reminders, Gmail sync notifications — all real Telegram sends via a raw HTTP call, deduped via `NotificationLog`/`ActionItemReminderLog`), but it's entirely legacy-content-driven and disconnected from V3's tool catalog/planner/executor, which had zero proactive code path before this pass. New `apps/api/src/operator/proactive.ts` exports `decideProactiveOperatorMessage(input)` — a pure function, no DB reads or sends — taking an already-loaded `ContextBundle` (same shape every chat turn already builds), `NotificationSettings`, the current time, and caller-computed dedupe-key/count state; returns `no_message` or a proposal (`type`, `title`, `message`, `reasons`, `suggestedReplies`, `dedupeKey`, `priority`, `safeToSend`). Wired to one route, `GET /users/:userId/operator/proactive/preview` (`apps/api/src/routes/agent.ts`), which reuses the unmodified `loadContext` and `hasNotificationLog`, never calling `createNotificationLog` or sending anything. Three moments, priority order, one returned per call: `morning_brief` (grounded top-3 open actions, one deferred low-priority item, one guardrail-risk watchout if a `risk_pattern` memory was logged that day, falling back to the exact goal-anchor-nudge text — now exported from `runtime.ts` — for a fully empty user), `evening_checkin` (only asks about goals with a real `targetMetrics[].eventType` not yet logged today, capped at 2, silent once logged), `gmail_nudge` (top pending review via the existing safe `gmailReviewChatLabel`/`gmailReviewChatDescription` helpers, deduped per-review). Anti-spam: `dailyLoopEnabled: false` suppresses everything (the available quiet-hours proxy — no dedicated field exists); `gmail_nudge` bounded to the user's own morning-to-evening window; a hard 1–3/day cap checked before any candidate is built; all dedup reuses `NotificationLog` unchanged, zero schema added. 11 new tests in `tests/agent-runtime-proactive.test.ts` (grounded morning brief, goal-anchor fallback, no invented facts, evening check-in logic both ways, safe Gmail summary, no-reviews case, dedupe-key repeat suppression, quiet-hours/daily-loop-disabled suppression, daily-cap suppression) plus 2 scripted-smoke scenarios (17: evening-checkin preview → natural reply → both events logged via existing tools, unmodified; 18: Gmail-nudge preview → "turn it into a task" → existing triage creates the action, unmodified) proving the module only decides *whether* to nudge, never reimplementing reply-handling. Full suite: **364/364**. See `docs/10-v3-readiness-audit.md`'s §13 for the full design and what remains before enabling real sending (worker wiring behind an explicit flag, session-entity delivery for bare Gmail-nudge replies, priority-aware cap behavior).
- [x] **V3 Proactive Operator MVP — morning_brief delivery.** Cautious first real-delivery path: real Telegram sends for exactly one proactive moment, `morning_brief`, off by default behind `PROACTIVE_OPERATOR_DELIVERY_ENABLED`. At this historical milestone, `evening_checkin`/`gmail_nudge` stayed preview-only; real `gmail_nudge` delivery is documented in the later Gmail nudge entry below. `GET /users/:userId/operator/proactive/preview` itself is untouched and still never writes `NotificationLog`. New `apps/worker/src/v3-proactive-delivery.ts` (kept separate from `apps/worker/src/index.ts`, which runs its tick loop as an import side effect and so is unsafe to import in tests — mirrors the existing `integration-sync.ts` separation pattern) exports `runV3ProactiveMorningBriefs(settings, options)`, wired into `index.ts`'s existing `runTick()` right after the legacy `runDailyMorningBriefs`/`runDailyEveningReviews`, reusing the same settings list already fetched that tick. For each `dailyLoopEnabled`, allowlisted user whose local time matches `morningTimeMinutes` (the identical gating the legacy morning brief already uses): calls the existing, unmodified preview route via `apiGet`; if the decision is `proposed_message` with `type === "morning_brief"`, sends via the existing `sendTelegramMessage`, then — only on a successful send — writes `NotificationLog` with the decision's own `dedupeKey` via the existing `createNotificationLog`. A failed send is logged and never marks the message sent, so it stays eligible to retry next tick. Three small datetime helpers (`formatLocalTime`/`formatLocalDate`/`formatMinutesOfDay`/`getPart`) were extracted from `index.ts` into a new `apps/worker/src/datetime.ts` so both files share identical logic without duplication or an unsafe import. New `PROACTIVE_OPERATOR_ALLOWLIST` env var (comma-separated userIds, optional — unset means every `dailyLoopEnabled` user is eligible once the master flag is on) — no allowlist mechanism existed anywhere before this pass, so a simple one was added; the master flag itself mirrors the existing `INTEGRATION_SYNC_ENABLED === "true"` pattern, the only precedent for this kind of flag in the codebase. 9 new tests in `tests/worker-v3-proactive-delivery.test.ts` (disabled sends nothing; enabled + real candidate sends exactly one message; `NotificationLog` written only after success; failed send never logs; duplicate dedupe key suppresses; `evening_checkin`/`gmail_nudge` candidates confirmed offered by the decision layer yet never sent; preview route confirmed to still write nothing across repeated calls; allowlist delivers to one user while skipping another) — all calling the real, unmodified preview route in-process via `server.inject` (no real network), stubbing only `sendTelegramMessage`. Full suite: **373/373**. See `docs/10-v3-readiness-audit.md`'s §14 for the full design and the manual smoke checklist for verifying against a real bot token.
- [x] **V3 Proactive Operator MVP — user-level consent layer.** Product correction: `PROACTIVE_OPERATOR_DELIVERY_ENABLED`/`PROACTIVE_OPERATOR_ALLOWLIST` are developer rollout controls, not the product UX — no proactive delivery is now possible for any user without their own explicit, confirmed opt-in. New per-user consent: three `NotificationSettings` booleans (`morningBriefEnabled`/`eveningCheckinEnabled`/`gmailNudgeEnabled`, all default `false` — Prisma migration `20260819224352_add_proactive_operator_settings`, purely additive), deliberately kept separate from the older, unrelated `dailyLoopEnabled` (which still only gates the legacy `/daily-loop/start-day`|`end-day` messages) to avoid overloading a setting whose meaning would otherwise become ambiguous. `decideProactiveOperatorMessage` (`apps/api/src/operator/proactive.ts`) is completely unchanged — still a pure content-decision module unaware of env flags or per-user consent, so every existing decision test (§13) passes unmodified. New `apps/api/src/operator/proactive-eligibility.ts` (`evaluateProactiveEligibility`) layers real send-eligibility on top, reported by the preview route as `wouldSend`/`blockedBy` (`delivery_disabled`/`not_allowlisted`/`user_not_opted_in`/`quiet_hours`/`dedupe`/`no_candidate`) — informational only, the route still never writes the DB. `apps/worker/src/v3-proactive-delivery.ts`'s actual send gate independently re-checks `morningBriefEnabled` itself before ever sending — defense in depth, not solely reliant on the preview route's own reporting. The two env-reading functions were extracted from `apps/worker` into `packages/core/src/proactive-operator-env.ts` so apps/api and apps/worker read the identical two env vars the identical way, without duplication. Three new V3 tools mirror `daily_loop.settings_*`'s exact propose→confirm shape — `proactive.settings_show`/`settings_propose_update`/`settings_apply_update` (the last never planned directly by the LLM, validator-rejected, only reached via the confirm whitelist) — supporting "turn on/stop morning briefs", "check in every evening"/"stop evening check-ins", "turn on/stop Gmail nudges", "what proactive messages are on?"; a request for finer control like "be stricter with this goal" or "less nudges please" is explicitly declined honestly, matching this session's established scope-boundary-honesty pattern rather than guessing which toggle is meant. "Alecto can suggest proactive support" is pure `planner.ts` prompt guidance — no new mechanism: when the planner recognizes a clearly daily/recurring goal, it may plan `proactive.settings_propose_update` in that same turn, which is structurally identical to any other settings proposal — it opens a pending confirmation and mutates nothing until the user replies with an exact "yes"; the LLM can never silently enable anything. 17 new tests across `tests/agent-runtime-proactive-settings.test.ts` (propose/apply on and off, independent per-toggle mutation, real-settings show, no-pending "yes", unspecified-field clarification, direct-apply rejection), `tests/agent-runtime-proactive-suggestion.test.ts` (a suggestion opens a pending confirmation and mutates nothing until confirmed; an ordinary non-recurring goal statement isn't forced to trigger one), `tests/agent-runtime-proactive-eligibility.test.ts` (all-clear `wouldSend: true`; each of the three blockers reported independently; content decision unaffected by opt-in status; preview route still writes nothing across repeated calls), plus one fixture update in `tests/worker-v3-proactive-delivery.test.ts` (`morningBriefEnabled: false` now suppresses a send even with delivery enabled and the user allowlisted) and scripted-smoke scenario 19 (goal statement → suggestion → confirm → real-settings check → toggle off → confirm, verified against final DB state). Full suite: **391/391**. See `docs/10-v3-readiness-audit.md`'s §15 for the full design.
- [x] **V3 Proactive Operator MVP — Gmail nudge delivery.** Real Telegram delivery now exists for `gmail_nudge` in addition to `morning_brief`, still off by default behind `PROACTIVE_OPERATOR_DELIVERY_ENABLED` and the per-user `NotificationSettings.gmailNudgeEnabled` opt-in. This does **not** add Gmail webhooks, all-inbox scanning, Gmail send/reply/label mutation, or a new review-creation path. The worker calls the existing preview route, sends only an already-created `EmailReviewItem` nudge when the decision type is `gmail_nudge`, stores the safe `gmail_review` visible entity in `AgentConversationSession` so the user's next normal V3 reply can say "turn it into a task" or "ignore it", then writes `NotificationLog` only after the Telegram send and session-context write succeed. The scheduled Gmail sync worker now suppresses the older bundled Gmail review notification only when the V3 preview says a Gmail nudge would actually send for that user; if V3 is disabled, not opted in, not allowlisted, quiet-hours-blocked, deduped, or preview fails, the legacy bundled notification remains the fallback. The downstream boundary is intentionally generic: Gmail reviews are today's concrete source object, but follow-up decisions remain V3 tool/actions such as create action, log goal evidence, reject/review, or ask clarification; Calendar is documented as a future integration source and no fake Calendar tools were added. New tests cover delivered nudge/log/session context, failed-send no log/no context, dedupe, opt-in/allowlist gates, no-allowlist delivery, existing V3 review-to-action reply flow after delivery, and legacy notification suppression/fallback. `evening_checkin` remains preview-only.
- [x] **V3 Proactive Operator MVP — settings/delivery UX bug fixes.** Real Telegram testing of the consent layer above found "setup a morning brief at 01:06" only set the time via the legacy `daily_loop.settings_propose_update` tool and never turned `morningBriefEnabled` on, so nothing was ever scheduled despite the bot replying "Done" — two tools each partially, independently controlled the same `NotificationSettings` row with no way to express "turn on AND schedule" as one action. Fix: `proactive.settings_propose_update`/`settings_apply_update` gained `morningTimeText`/`morningTimeMinutes` (and the evening equivalents), so one tool now owns both the on/off consent and the scheduled time; a combined request ("set up a morning brief at 9am") proposes and applies both as one fused change ("You're about to turn on the morning brief at 09:00...")  rather than two disjointed phrases, and a time-only request while the moment is off still says so explicitly ("...but the morning brief is still off"). `proactive.settings_show` now includes the scheduled time when a moment is on. New read-only tool `proactive.diagnose_morning_brief` answers "why didn't I get my morning brief?" with a single, most-specific, grounded status — `delivery_disabled`/`user_not_allowlisted`/`user_not_opted_in`/`daily_loop_disabled`/`outside_morning_window`/`duplicate_dedupe_key`/`no_candidate`/`eligible` — via a new `getProactiveDeliveryStatus` in `apps/api/src/operator/proactive-eligibility.ts`, replacing the previous generic settings-comparison non-answer ("That's already how it's set") for that question. It reuses `decideProactiveOperatorMessage` and its exported time-window primitives (`isWithinWindow`/`minutesOfDayInTimezone`/`TIME_TRIGGER_WINDOW_MINUTES`) directly, so it can't drift from either the content-decision module or `apps/worker`'s own send gate — the relationship (and one intentional difference: the worker's exact-minute tick trigger vs. this diagnostic's ±window check) is documented in both `apps/worker/src/v3-proactive-delivery.ts` and `proactive-eligibility.ts`, since the two packages can't share one function across their HTTP-only boundary. `GET /users/:userId/operator/proactive/preview` additionally reports `scheduledTime`/`optIn`, purely additive, still writing nothing. 11 new/updated tests across `tests/agent-runtime-proactive-settings.test.ts`, a new `tests/agent-runtime-proactive-diagnosis.test.ts` (one per reachable status), and `tests/agent-runtime-proactive-eligibility.test.ts`. Full suite: **401/401**. See `docs/10-v3-readiness-audit.md`'s §16 for the full design.
- [x] **Betting/trading/finance domain-specificity audit.** V3 has no hardcoded gambling/trading/finance classifier: `apps/api/src/agent-runtime/goal-guardrails.ts` was already fully domain-agnostic before this pass, classifying conflicts only against the user's own active goals and configured `knownTriggers`/`knownFailureModes` — gambling/trading appear only as illustrative examples a user might pick, never as a built-in special domain. Triggered by a real morning brief mentioning `"Guardrail: Keep Control impulsive betting locked today."`, traced conclusively to a real, active `Goal` row (confirmed present in the connected dev DB, not a code default) surfaced through the **legacy** `apps/api/src/operator/attention.ts` daily-brief cluster via `packages/core/src/daily-priority.ts`'s hardcoded `isRiskControlGoal` keyword classifier — not V3's `decideProactiveOperatorMessage`, which was confirmed never to generate that phrasing and only ever surfaces a generic `risk_pattern` memory. One real V3 finding was fixed: `runtime.ts`'s empty-user goal-anchor nudge previously suggested `"Control impulsive spending/gambling/trading"` as a default example goal; changed to the neutral `"Avoid impulsive spending"`. Test files that reused gambling/betting as their default recurring example (`agent-runtime-goal-guardrails.test.ts`, `agent-runtime-scripted-smoke.test.ts`, `agent-runtime-goal-listing.test.ts`, `agent-runtime-goal-anchor-nudge.test.ts`, `agent-runtime-action-hygiene.test.ts`) were diversified to neutral domains, keeping exactly one canonical "clearly seeded as a user goal" gambling example per the task's own guidance. 4 new tests added proving there is no built-in "betting = block" behavior and that the morning brief only ever mentions betting/finance when real `risk_pattern` data exists. Legacy's `isRiskControlGoal` classifier (real, hardcoded, used across 10+ legacy files, still the actual sender behind the real brief that prompted this audit) was audited and reported in full but deliberately not modified this pass — out of scope per the task's own "active V3 code" framing and the size of its blast radius; flagged as a scoped follow-up. Full suite: **405/405**. See `docs/10-v3-readiness-audit.md`'s §17 for the complete trace and audit findings.
- [x] **Legacy daily-loop vs V3 proactive morning delivery — ownership and duplicate-send prevention.** A real Telegram smoke test's morning message turned out to come from the legacy daily-loop sender (§17 above), not V3 — auditing why revealed both systems are driven by the identical `NotificationSettings` row (`dailyLoopEnabled` + `morningTimeMinutes`) in the identical worker tick, with nothing stopping both from sending once V3 real delivery is ever turned on for a user. Ownership decision: "morning brief" in chat now always means V3's proactive morning brief, never the legacy daily loop — the planner prompt, `daily_loop.*` tool-catalog descriptions, and the legacy tool's own reply text (renamed "start-day message") were all purged of the ambiguous "morning brief" phrasing that let a real request route to the wrong tool. Duplicate-send prevention: `apps/worker/src/legacy-daily-loop-morning.ts` (extracted from `apps/worker/src/index.ts` for testability, mirroring the existing `v3-proactive-delivery.ts` pattern) now skips the legacy send whenever V3 is genuinely configured to own delivery for that user — `morningBriefEnabled` AND `PROACTIVE_OPERATOR_DELIVERY_ENABLED` AND allowlisted — never just because the user opted into the V3 toggle alone, so turning on `morningBriefEnabled` can never silently leave a user with zero morning message while V3 real delivery isn't actually live for them. `proactive.diagnose_morning_brief` gained a new highest-priority `legacy_daily_loop_sent_instead` status so "why didn't I get my morning brief?" can never imply V3 sent something legacy actually delivered. Legacy daily-loop remains the live, real default sender for virtually all users; nothing about who V3 sends to changed. 8 new tests: `tests/worker-legacy-vs-v3-morning.test.ts` (4 — no duplicate send, legacy skipped when V3 owns delivery, legacy still sends when V3 isn't actually live, legacy still sends when not allowlisted), `tests/agent-runtime-planner-morning-brief-routing.test.ts` (3 — prompt-content regression guard, since real LLM tool choice isn't deterministically testable), plus 1 new diagnostic test; `tests/agent-runtime-daily-loop-settings.test.ts` updated for the renamed reply wording only. Full suite: **413/413**. See `docs/10-v3-readiness-audit.md`'s §18 for the complete system map, ownership rationale, and what remains before legacy code can be deleted.
- [x] **Simplified proactive rollout controls — optional, normalized allowlist.** Real solo/dev testing hit `PROACTIVE_OPERATOR_ALLOWLIST=520894688` not matching the internal `telegram:520894688` userId — a config-format mismatch, not a code bug, but real friction with no real users yet and per-user opt-in already the main safety layer. Rollout model clarified: `PROACTIVE_OPERATOR_DELIVERY_ENABLED` stays the required global kill switch; per-user opt-in stays required product consent; `PROACTIVE_OPERATOR_ALLOWLIST` is now explicitly **optional** (unset/empty was already non-blocking — that didn't change — but is now genuinely ergonomic). New `packages/core/src/proactive-operator-env.ts` `normalizeProactiveAllowlistId` accepts both a bare Telegram numeric id and the full `telegram:<id>` form, comma-separated, whitespace-trimmed; new `proactiveOperatorAllowlistActiveFromEnv()` separates "an allowlist is configured" from "this user matches it." The dead, never-reachable `missing_allowlist` diagnostic status was removed; the `eligible` diagnosis now plainly states the allowlist's active/inactive state instead of leaving it ambiguous. `GET /users/:userId/operator/proactive/preview` additionally reports `deliveryEnabled`/`allowlistActive`/`allowlistMatched`. New worker startup log reports the two rollout controls' actual live state in that process, closing the exact visibility gap that made the original debugging take several round-trips. New `tests/proactive-operator-env.test.ts` (10 unit tests) plus additions to `tests/agent-runtime-proactive-eligibility.test.ts` and `tests/agent-runtime-proactive-diagnosis.test.ts`. Full suite: **432/432**. See `docs/10-v3-readiness-audit.md`'s §19 for the complete design.
- [x] **Generic Goal Evidence Loop MVP.** Reframed mid-pass from an initial "Job Search Operator Loop" ask into an explicitly generic architecture — Goal + Evidence + Source + Confidence + Suggested Mutation — with job search as only the first goal category exercised, never a hardcoded module. Almost all of it already existed as generic primitives (a Goal's own `targetMetrics[].eventType`, the domain-spanning event-registry, `ActionItem.goalId`/`EmailSignalRule.goalId`, `inferGoalLinkForAction`'s confidence-scored action↔goal matcher, `approveEmailReviewForUser`'s classification→event/action pipeline) — this pass mostly exposed it to normal V3 chat rather than inventing new mechanisms. New: `packages/core/src/goal-evidence.ts` (`findGoalsForEventType`/`describeGoalEvidenceMatch`); new V3 tools `goal.log_evidence` (logs a concrete signal beyond plain application counts/workouts, and names the goal it counts toward — enum is career-scoped today only because that's the first acceptance domain, architecturally generic), `goal.status` (grounded per-goal progress: linked actions, evidence counts this week/today, pending linked Gmail reviews — same code for any goal), and `gmail.review.approve` (exposes the existing generic approval pipeline to chat, alongside the unchanged `gmail.review.to_action`); `action.create` now auto-links to a matching active goal, same matcher email-review-to-action already used; the V3 morning brief gained one generic line surfacing a goal-linked pending Gmail review. Acceptance suite (`tests/agent-runtime-goal-evidence.test.ts`, 9 tests): 1–8 use job-search examples ("sent 5 CVs," "got 2 recruiter replies," "I have an interview tomorrow," "need to follow up with recruiter tomorrow," job-search status, a recruiter Gmail review becoming a real logged event, the morning brief surfacing it, and an honest "no active goals" when none exist); **test 9 deliberately proves genericness** with an unrelated "Handle Endesa bills" goal linked to a matching Gmail review through the identical tools, zero job-search-specific code involved. Manual only: nothing auto-detects job-search (or any) activity outside an already-classified Gmail review; scheduled Gmail monitoring and `gmail_nudge` delivery were not wired by this historical pass, but both are implemented by later Gmail autonomy/proactivity work documented above; `evening_checkin` remains preview-only. Full suite: **441/441**. See `docs/10-v3-readiness-audit.md`'s §20 for the complete design and what a future goal domain needs to reuse this (nothing architecturally new — just registry/template/enum additions).
- [x] **Adaptive Goal Creation / Goal Operating Plan MVP.** Product correction: "future goals reuse this with registry/template/enum additions per domain" wasn't good enough — Alecto must handle an arbitrary user goal ("I want to drink more tea," "call my grandmother every Sunday," "stop scrolling in bed") without a developer adding a new EventType/template first. Key finding: `Goal.targetMetrics` is a Postgres `Json?` column and `GoalMetricSchema.eventType` was already an unconstrained string, not locked to the EventType registry — so this needed zero database migration, only one new optional Zod field (`signalKey`) alongside the existing `eventType`. Custom-signal evidence reuses the existing generic `custom.goal_progress_logged` event type with `data.signalKey` as the real discriminator. New V3 tools: `goal.create_propose` (LLM proposes a full custom operating plan — title/category/why/successCriteria/1-3 invented signal keys+labels/check-in/integration hint/first actions — deterministically formatted, never raw LLM prose, opens a pending confirmation like every other propose→confirm flow in this codebase), `goal.create_apply` (internal-only, rejected if planned directly, calls the existing unmodified `createGoal`), `goal.tracking_show` (shows real configured signal keys). `goal.log_evidence` now accepts `signalKey` as well as the fixed career `eventType` enum, verified deterministically against the user's real active goals before logging anything — an unrecognized key is refused, never guessed. `goal.status`'s counting was reworked to iterate a goal's own declared metrics uniformly via a new `countEvidenceForMetric` helper. New `tests/agent-runtime-goal-creation.test.ts` (9 tests) runs the full desired flow end to end — propose the tea goal, confirm it, log "had 2 teas today" against the custom signal, refuse an undeclared signal key, show real tracking config, report real progress, handle a duplicate confirmation honestly, reject direct `goal.create_apply` planning, and never fabricate a plan for a vague statement. Two pre-existing tests were intentionally updated from the old "goal creation isn't supported" decline behavior to the new propose→confirm behavior — an intentional product change, not a regression. Full suite: **450/450**. See `docs/10-v3-readiness-audit.md`'s §21 for the complete design.
- [x] **Goal Reference Fix — specific-goal resolution, evidence/status consistency, reading-goal plan quality.** A real Telegram smoke test of the Adaptive Goal Creation MVP above found that with more than one active goal, `goal.status`/`goal.tracking_show` kept resolving a named follow-up ("show tracking for reading niezsche book," a typo of "Nietzsche") to an older, generic, same-category goal ("Read more") instead of the goal actually named — traced to both tools reusing `inferGoalLinkForAction`, a matcher built for a different job (which goal's general DOMAIN a new *action* belongs to) with coarse category buckets and no typo tolerance. Separately, logging "read 5 minutes" produced a confident "I've logged that" reply that `goal.status` never actually counted — traced to `goal.log_evidence`'s `eventType` being a hardcoded 5-value career-only enum that couldn't accept a template goal's real metric (e.g. "Read more"'s `learning.reading_session_completed`), pushing the planner to the untargeted, never-goal-linked `event.log_custom_progress` instead, whose success then let `composeReply`'s (correct, unchanged) "trust a mutating tool's own replyDraft" rule surface a false-sounding claim. Fix: a new, dedicated `packages/core/src/goal-reference.ts` resolver (`resolveActiveGoalReference`) — kept deliberately separate from `goal-linking.ts`, which is untouched and still used for its original action-linking job — scores a specific/distinctive title match above a generic category match, tolerates common misspellings of a proper noun generically (Levenshtein edit distance, not a hardcoded name check), and returns `"ambiguous"` (asks which goal) instead of guessing when goals are genuinely tied. `goal.status`/`goal.tracking_show` now use it, with an honest "I couldn't find a goal matching X" for a genuine non-match instead of the old silent fallback to every active goal. `goal.log_evidence`'s `eventType` is now a generically-verified real registered event type instead of a fixed enum, and gained an optional `goalRef` for disambiguation. `goal.create_apply` now includes the new goal in the turn's visible entities so an immediate "show tracking for it" resolves correctly. `planner.ts`'s guidance now asks for an ongoing progress signal alongside a completion signal for "finish X"-shaped goals (books, courses, projects — generic, not reading-specific), and steers the LLM toward `goal.log_evidence` over the untargeted fallback tool. New `tests/agent-runtime-goal-reference.test.ts` (6 tests) reproduces the exact reported scenario across three real typos, an ambiguous-reference clarification, immediate post-creation visibility, a refused nonexistent event type (proving a failed call never leaks a false-success `replyDraft`), and a two-signal adaptive book-goal plan. Full suite: **456/456**. See `docs/10-v3-readiness-audit.md`'s §22 for the complete design.
- [x] **Goal Conversation Reliability — session goal focus, guardrail progress-report exemption, optional real-LLM transcript evals.** A second real Telegram smoke test (two reading goals active) found the very next message after correctly showing tracking for the OLDER goal by name got intercepted by the goal-aligned guardrail and misclassified as a "lapse" against the NEWER goal instead — traced to (a) nothing tracking which goal the conversation was currently "about" (a bare/omitted reference fell back to the newest-created goal, not the one just shown), and (b) the guardrail's LLM classification tier having no concept of conversational focus either, plus treating a partial progress report ("I read 30 minutes today" after "I didn't finish it") as lapse-shaped, when reporting real progress is not sabotage. Fix: a new sticky `session.focusedEntities` slot (backed by a previously-reserved, always-null DB column) updated whenever `goal.status`/`goal.tracking_show`/`goal.log_evidence`/`goal.create_apply` resolve to exactly one goal, preferred over "newest goal" for a pronoun reference and as an ambiguity tiebreaker, and survives any turn that doesn't touch a goal at all; a new deterministic `looksLikeProgressReport` check in `goal-guardrails.ts` that short-circuits a quantified activity report or explicit "log it" phrasing straight to `allow` BEFORE the LLM classification tier runs (an explicit user-configured trigger still always hard-stops regardless; a genuinely non-progress message still reaches classification); and `ensureBookGoalProgressSignal` (`packages/core/src/goal-evidence.ts`), which now DETERMINISTICALLY injects a title-derived, unique progress signal into any completion-only book/reading proposal (prompt guidance alone, from the prior pass, didn't reliably produce one), plus a `goal.log_evidence` fallback that remaps an LLM-guessed generic key to a goal's real compatible metric when one exists, or honestly offers to add one when it doesn't, instead of silently dropping evidence or logging it unlinked. Also new: an OPTIONAL real-LLM multi-turn transcript eval harness (`tests/agent-runtime-llm-eval.test.ts` + `tests/helpers/llm-eval-helpers.ts`, 4 scenarios) that runs the actual planner/guardrail (zero mocks) through `POST /agent/message`, scored on reply properties and real DB evidence rather than exact-text snapshots — off by default via node:test's `skip` option (needs `RUN_LLM_EVALS=true` + `OPENAI_API_KEY`; new `pnpm test:llm` script), a failing scenario dumps a full transcript trace to a gitignored local directory. New `tests/agent-runtime-goal-conversation-reliability.test.ts` (8 deterministic tests) reproduces the exact transcript shape, focus surviving to a later log turn, the guardrail exemption proven against a deliberately wrong mocked classification (for both a quantified report and "log it"), a genuinely non-progress message still reaching and being blocked, signal injection, ambiguity-resolved-by-focus, and the ask-to-add-signal fallback. Full deterministic suite: **464/464** (456 before this pass + 8 new); `pnpm test` overall: 464 passed, 4 skipped (the LLM evals, no API key in this environment), 0 failed. See `docs/10-v3-readiness-audit.md`'s §23 for the complete design.
- [x] **LLM eval suite expanded into a product-conversation QA harness.** Once `OPENAI_API_KEY` was actually available, running §23's 4 scenarios for real immediately caught 4 more real bugs the deterministic suite structurally cannot see (a guardrail false positive on "I also want to track a second goal," `goal.log_evidence` blindly preferring a hedged `eventType` over the target goal's real `signalKey`, a missing-signal fallback gap when the planner supplies neither field, and `event.log_custom_progress` chosen over `goal.log_evidence` producing unlinked, overclaimed evidence) — each closed with a real executor/guardrail-prompt/response-composer fix, confirmed against the same real scenarios afterward. Product decision: keep this as an explicitly OPTIONAL, manually-run QA tool, never default CI — `tests/agent-runtime-llm-eval.test.ts` stays in `pnpm test`'s glob (so it's always typechecked) but every scenario skips via a new tag-aware `llmEvalOptions(tags)` unless `RUN_LLM_EVALS=true` + `OPENAI_API_KEY` are both set. Expanded from 4 to 10 scenarios covering Alecto's highest-value journeys: onboarding, an adaptive rare goal (tea), a book goal (the original reported shape), multi-goal ambiguity, the job-search evidence loop, both Gmail flows (job-linked and fully generic admin/bills, proving no job-search assumptions leak), the guardrail-vs-progress-report boundary, proactive settings (checked directly against the DB, not just reply wording), and morning brief preview quality. Tooling: `LLM_EVAL_TAGS=gmail,job-search pnpm test:llm` runs only matching scenarios; a failing scenario's trace now also records every checkpoint attempted (not just the first failure) plus a final DB state snapshot (goals/actions/events/Gmail reviews/notification settings); new `assertActionCreated` ground-truth helper. No new deterministic bugs found this pass — a real run scored 9/10 (the one failure was a transient real-API error falling over to the heuristic fallback planner, confirmed non-reproducible via two isolated re-runs, not a logic bug). Full deterministic suite unaffected: **464/464**; `pnpm test` overall: 464 passed, 10 skipped, 0 failed. See `docs/10-v3-readiness-audit.md`'s §24 for the complete design and scenario list.
- [x] Short-lived interaction context for visible action/hygiene lists, recent action mutation status, focused Gmail rules, pending Gmail proposals, email review lists, and plan sessions through the existing pending-action layer
- [x] `/messages/process` routeDebug metadata for API smoke tests: router source, intent, handler, semantic-router usage, mutation flag, confidence, language, side-effect risk, confirmation requirement, and compact reason
- [x] User onboarding/setup simplification v1: shared API onboarding state/reply composer, `/start`, `/setup`, natural quickstart, missing setup, goals setup, daily-loop setup, and integration setup guidance
- [x] First 5 Minutes Onboarding v1: guide-style `/start`, setup overview with Ready/Needs attention/Optional/Best next step, state-aware next-step suggestions, action/reminder onboarding, and explicit Gmail/GitHub setup boundaries
- [x] Inbound message segmentation for single commands, command batches, reference text, and normal text
- [x] Safe command batches for read-only commands and selected action write commands
- [x] Reference/log/code-fence safety so pasted command examples are not executed
- [x] Worker process for proactive jobs
- [x] Product capability audit and command-surface map in `docs/08-product-capability-audit.md`
- [ ] Web UI/dashboard
- [ ] WhatsApp channel adapter
- [ ] OpenClaw gateway adapter
- [ ] Mobile app
- [ ] General OAuth/auth layer beyond Gmail local MVP

## User Profile And Memory

- [x] User operating profile with directness, warmth, confrontation, style, guardrail, and vulnerability preferences
- [x] `/set_style hard_guardian` and `/set_style balanced`
- [x] Explicit memory creation from `/remember` and natural memory phrases
- [x] Memory archive via `/forget_memory`
- [x] Pending memory suggestions for inferred patterns
- [x] Duplicate pending memory suggestion prevention
- [x] Operator reflection generation/list/archive/debug commands
- [~] Operator reflections are deterministic/local-MVP; no vector DB or embeddings
- [ ] Semantic memory retrieval through embeddings/vector search

## Goals

- [x] Goal CRUD basics and archive
- [x] Goal templates for job search, health, sleep, reading, finance/risk, deep work, creative/build, and social/custom shapes
- [x] Natural-language goal creation with confirmation
- [x] Custom goal config with metrics, check-in questions, and progress logging
- [x] Duplicate active goal warnings
- [x] Goal priority fields: `priority`, `importanceScore`, `priorityReason`
- [x] Default goal priority backfill with template/title aliases
- [x] `/goal_priorities`, `/set_goal_priority`, and `/debug_backfill_goal_priorities`
- [x] Goal-linked ActionItems
- [x] Completing linked actions creates generic goal-progress evidence without inventing domain facts
- [~] Goal matching is deterministic/rules-first and intentionally conservative

## Events And Check-Ins

- [x] Core event registry in `docs/02-event-ontology.md` and `packages/core`
- [x] Event extraction from chat
- [x] Structured `/checkin`
- [x] Natural-language check-in parsing in English/Spanish
- [x] Goal-aware daily check-in prompt builder
- [x] Daily check-in reminders through the worker
- [x] Event archive/delete/undo/correction
- [x] Event correction validation by event type
- [x] Parent daily-check-in answer sync when correcting derived check-in events
- [x] `/events [limit]` and `/events_archived [limit]` with Telegram-safe truncation
- [x] Daily factual `/review`
- [x] Daily review sums progress metrics and uses latest state metrics
- [~] Custom event types can be proposed conceptually, but only approved ontology types should be logged automatically

## Risk And Guardrails

- [x] Deterministic betting/trading risk detection
- [x] Guardian RED hard-stop behavior
- [x] Direct betting/trading intent takes priority over check-ins and generic chat
- [x] Risk guardrails for natural action creation and slash action commands
- [x] Multi-intent guardrail precedence
- [x] Cooldown events for RED betting/trading states
- [x] Risk-control goals excluded from normal stalled-goal lists and summarized as guardrails
- [ ] Crisis/BLACK-state protocol beyond current betting/trading guardrails

## Action Items

- [x] `ActionItem` model with manual/email/system sources, status, due/snooze fields, priority, project, action type, and goal link
- [x] Manual commands: `/action`, `/todo`, `/add_action`
- [x] Natural action intake
- [x] Shared title normalization and dedupe for manual actions
- [x] Natural date parsing with local timezone, reminder preferences, `now`, vague future rolling, and explicit-past rejection
- [x] `/actions`, `/actions all`
- [x] `/complete_action`, `/snooze_action`, `/archive_action`
- [x] Action reminder worker with 12-hour duplicate suppression
- [x] Dev helpers for due-action reminder testing
- [x] Conversational action control for complete/reschedule/snooze/archive/priority changes
- [x] Pending decisions for ambiguous or destructive conversational actions
- [x] Multi-intent conversational orchestrator
- [x] Action hygiene analyzer, `/action_hygiene`, `/debug_action_hygiene`, and hygiene-session replies
- [x] Hygiene reply hardening: sessions stay active across completed/snoozed candidates, incomplete snooze replies ask for a time, and generic chat cannot fake cleanup success
- [x] Conversational Action/Hygiene Control v2: visible-number and natural-label resolution for hygiene/action lists, batch archive/complete/snooze/keep plans, all-except cleanup replies, confirmation before destructive batches, and short-lived recent mutation answers
- [x] ~~Conversation Orchestrator v2 golden transcript coverage~~ — retired along with v2 itself; see the Conversation Orchestrator v2 retirement entry above. This test coverage (action hygiene list creation/replies, batches, all-except cleanup, single-item pronoun replies, recent mutation status, explicit memory creation, multilingual references/progress aliases, natural confirmation variants, cross-domain safety, risk precedence, LLM planner success/fallback/timeout/debug fields) has no legacy equivalent and was not preserved, since it tested v2-specific mechanics that no longer exist.
- [~] Snooze history/count is inferred from available state where possible; no full action history model yet

## Daily And Weekly Operator Loop

- [x] `/today` daily operator brief
- [x] Natural daily operator requests such as `what should I do today` and `start my day`
- [x] Natural attention requests such as `anything important?`, `what needs my attention?`, `what should I handle first?`, `what emails need action?`, and Spanish/Catalan Gmail-attention variants route to operator/email attention summaries instead of generic coaching
- [x] Daily priority scoring with goal priority weights
- [x] `/debug_daily_priorities`
- [x] Optional LLM Daily Coach with strict validation and deterministic fallback
- [x] `/debug_daily_coach`
- [x] `/start_day`, `/end_day`, `/tomorrow`
- [x] Daily loop settings and scheduled morning/evening worker delivery
- [x] Debug start/end-day send commands with idempotency and force mode
- [x] Local timezone day/week handling, defaulting to `Europe/Madrid`
- [x] Weekly operator review: `/weekly`, `/weekly force`, `/weekly_last`, `/debug_weekly_context`
- [x] Week-to-date weekly review display for current week
- [x] Natural `/review` and `/today` use consistent user-local day ranges; natural weekly review refreshes stale current-week memories through the reviewed local date
- [x] Weekly review stored as `MemoryEntry` with `data.kind="weekly_review"`
- [x] Pending Gmail reviews are included in `/today`, `/start_day`, `/end_day`, `/weekly`, `/debug_weekly_context`, and next-week planning context as review inbox work, not as fake ActionItems
- [x] `/plan_next_week` proposes 3-7 next-week ActionItems and creates only explicitly selected items
- [x] Plan-next-week output distinguishes ActionItem priority from linked goal priority
- [x] Plan-next-week cleanup suggestions are non-creatable and point back to `/action_hygiene` or natural action control instead of creating meta cleanup tasks
- [x] Planning UX Consolidation v1: `/weekly` points to `plan next week`, natural `plan this week`/`plan my week` plan the remaining current local week, natural `plan next week` uses the next local week, ambiguous `make a plan` asks for this-vs-next-week unless there is active plan context, `create all new` creates only creatable suggestions, reply examples only reference creatable indexes, and recurring system suggestions use semantic duplicate keys such as guardrail-review aliases
- [x] `/debug_next_week_plan_context` shows next-week planning context without side effects

## Insights

- [x] Daily interpretive `/insight` and `/daily_insight`
- [x] Weekly interpretive `/weekly_insight`
- [x] Insight reports use active events/goals/memories/profile and risk signals
- [x] Custom goal progress counts as real activity
- [x] GitHub repo activity does not count as personal progress
- [~] Optional OpenAI polish is additive only and falls back deterministically

## Notifications And Worker

- [x] `NotificationSettings`
- [x] `NotificationLog` duplicate prevention for daily check-ins, daily insights, and weekly insights
- [x] Scheduled daily check-ins
- [x] Scheduled daily insight delivery
- [x] Scheduled weekly insight delivery
- [x] Scheduled daily loop morning/evening briefs
- [x] Action reminder logs and due/snoozed reminders
- [x] Scheduled integration sync, disabled by default with `INTEGRATION_SYNC_ENABLED=false`; Gmail background sync additionally requires explicit per-user scheduled mode and active Gmail rules
- [~] Telegram delivery requires `telegramUserId`; no multi-channel notification routing yet

## Integrations

- [x] Integration registry
- [x] Integration lifecycle: pause/resume/archive
- [x] Duplicate GitHub connection prevention
- [x] Public GitHub repo integration
- [x] GitHub `author=LOGIN` personal commit semantics
- [x] GitHub repo activity semantics when no author is configured
- [x] GitHub safe 404/private/rate-limit errors
- [x] Scheduled and manual integration sync
- [x] Gmail background sync eligibility/debug route for safe operator inspection: global worker availability, connection status, mode, interval, active rule count, last background attempt, next due time, and reason
- [x] Natural `sync Gmail`, `sync email`, and `sync integrations` requests route through safe API sync behavior instead of generic chat
- [x] Natural explicit email-rule requests such as `enable job search rule for Gmail` and `enable work action rule for Gmail` create/reuse active Gmail email rules through the same safe API path as `/enable_email_rule`
- [x] Gmail setup/rule replies use human tracking labels and explain what the rule watches for instead of exposing adapter IDs in normal user output
- [x] Natural Gmail setup/status/list wizard shows connection state, active tracking rules, manual vs scheduled sync, email-rule timing behavior, and goal-based rule recommendations
- [~] GitHub supports public repos only; no OAuth/private repo support
- [ ] Wallet public-address fetcher
- [ ] Health integrations
- [ ] Calendar integrations

## Gmail And Email

- [x] Gmail listed as generic readonly email source
- [x] Minimal Gmail OAuth callback for local MVP
- [x] Gmail OAuth tokens encrypted at rest in local DB config when `ALECTO_SECRET_ENCRYPTION_KEY` is configured
- [x] Existing legacy plaintext Gmail token config can be read and migrated to encrypted config on successful token read/sync when the key is available
- [x] Tokens and encrypted token envelopes are sanitized from all user/API-visible output
- [x] Email signal rules with query, fetch strategy, classifier mode, caps, thresholds, and review-before-logging
- [x] Email adapter registry
- [x] `job_search_email` adapter
- [x] `work_action_email` adapter
- [x] Conservative Gmail hard filters for security, marketing, account/admin, and non-work noise
- [x] Gmail security/auth/account noise is filtered before job-search classification and before custom review creation. Security-code, verification-code, OTP, login, password-reset, 2FA/authentication, suspicious-login, device-login, and account-recovery emails do not create EmailReviewItems in v1.
- [x] Gmail sync summaries and debug counters
- [x] Natural Gmail/email sync requests use the same readonly sync path and preserve token/ciphertext redaction
- [x] Agent Runtime v3 normal-chat Gmail connect/reconnect/status requests reuse the real OAuth URL builder. Expired authorization replies say to reconnect and include the link when OAuth env is configured; tokens, ciphertext, IVs, tags, and raw provider errors are not exposed.
- [x] Agent Runtime v3 normal-chat Gmail nudge preference requests (`turn on Gmail nudges`, `stop Gmail nudges`) update only `NotificationSettings.gmailNudgeEnabled` after exact confirmation and never run Gmail sync. If Gmail auth is expired or disconnected, the reply says reconnect/connect is required before nudges can work.
- [x] Natural Gmail email-rule enable requests require an active Gmail connection, preserve explicit rule approval, and do not auto-scan by themselves
- [x] Natural Gmail guidance explains implemented tracking choices, including confirmation-first custom sender/keyword tracking
- [x] Gmail setup/status replies recommend job-search tracking for active career/job-search goals and work-action tracking for work/project-like goals
- [x] Shared Gmail setup/autonomy state service for conversation surfaces. It reports connection status, account email when available, active/paused rule summaries, pending review count, last sync, manual/scheduled mode, worker availability, review-notification preference, delivery availability, Gmail-relevant active goals, recommendations, unsupported preferences, and next best step without exposing secrets or raw provider data.
- [x] Gmail autonomy preferences on Gmail connection config: `manual_only` vs `scheduled`, interval minutes, and review-waiting notification on/off. Natural changes such as `check Gmail every hour`, `make Gmail manual only`, and `notify me when Gmail reviews are waiting` ask for confirmation before DB mutation.
- [x] Gmail autonomy confirmation focus is hardened: negative notification phrases disable notifications, unrelated Gmail questions cancel stale preference confirmations, and missing/paused custom-rule questions explain review-first/no-auto-log behavior instead of returning a generic "could not identify" message.
- [x] Worker scheduled Gmail sync respects Gmail autonomy preferences: Gmail stays manual-only unless the user explicitly chose scheduled checks, manual-only Gmail connections are skipped, per-connection scheduled intervals use `lastBackgroundSyncAttemptedAt` instead of manual `lastSyncedAt`, no-active-rule connections are skipped, failed background attempts store safe status/error without tight retry loops, and proactive Gmail review notifications are suppressed when the connection preference disables them.
- [x] Natural Gmail digest and work-hours preference requests are recognized and answered honestly as not implemented; Alecto does not create fake settings.
- [x] Natural email-rule list requests such as `what email rules are on now` show active rules first and hide paused/error noise from the normal conversation view
- [x] Custom Gmail sender/keyword rule builder v1 through natural chat, e.g. `track Endesa bills from Gmail` or `track emails from client@example.com for dashboard project`
- [x] Custom Gmail rules are review-only: matches create EmailReviewItems and never auto-create Events or ActionItems
- [x] Custom Gmail rule management through natural chat for clear pause/resume/remove requests, with confirmation before removal
- [x] Pending custom Gmail rule conversation state: natural English/Spanish/Catalan edits to `looks for` keywords, replacement wording such as `Aigues de Barcelona instead of Endesa`, `solo Aigues de Barcelona, no Endesa`, and `en lloc de`, questions about where matches go, timing/sync questions, goal-link correction/removal, cancel, and confirmation before scanning
- [x] Active custom Gmail rule editing through natural chat: after rule list/status/edit replies, Alecto keeps short-lived rule context so follow-ups such as `make that rule look for only Aigues de Barcelona instead of Endesa`, `when will you tell me about it?`, `pause it`, and Spanish/Catalan equivalents resolve to the focused rule
- [x] Natural Gmail timing/status questions such as `when do u check my gmail?`, `when do u check my mail`, `do you check Gmail automatically?`, and `will you notify me about emails?` route to sync timing guidance before broad custom-rule creation or LLM semantic routing
- [x] Ambiguous custom Gmail rule management stores pending clarification before routing falls back. Example: `elimina Endesa` can be resolved by replying `1` or `Endesa emails`, then destructive removal asks for yes/no confirmation.
- [x] Multi-rule Gmail removal asks a bulk confirmation and archives matched Gmail email rules only after confirmation. Example: `elimina Aigues de Barcelona y Endesa`.
- [x] Gmail rule list/reset phrases avoid action-control and multi-intent fallback. Examples: `what email rules do we have`, `delete all email rules`, `turn all off and delete them`, and contextual `can u delete all of em?` after listing rules route to Gmail status/cleanup and never produce "I could not confidently match that to an open action."
- [x] Built-in Gmail rule duplicate cleanup is implemented. Natural lists and destructive confirmations group equivalent duplicate job-search/work-action rules, Gmail sync ignores exact duplicate built-in rules, and enabling a built-in rule reuses/reactivates one existing rule while archiving duplicate built-in copies.
- [x] Natural reset can archive all visible Gmail email rules, including job-search, work-action, custom, and paused rules, after confirmation. It does not delete the Gmail connection or historical email reviews/events.
- [x] Exact custom Gmail rule names ending in `emails` are preserved during selection, so `Endesa emails` can resolve that rule instead of collapsing to the broader `Endesa` target.
- [x] Utility/bill custom Gmail tracking avoids false health-goal links; Endesa/Aigues bill rules do not attach to `Improve strength and energy` unless an actual utility/expense goal exists.
- [x] Gmail-rule cross-context safety for targeted follow-ups. A target-specific request such as `ignore the Endesa ones` does not fall back to unrelated visible rule context such as work-action emails; it asks the user to show email reviews or Gmail rules when no matching Endesa context is visible.
- [x] LLM semantic-router API smoke coverage for multilingual Gmail rule list, custom-rule creation, pending-rule edits, timing questions, pause/resume management, and guardrail precedence when an LLM mock misroutes risky text
- [x] Expired pending confirmations no longer block fresh full-message requests such as `create a rule for Endesa bills`
- [x] Conversation repair for bad routing around pending Gmail rules; Alecto acknowledges the specific pending-rule problem instead of falling into generic support/coaching
- [x] Email review queue
- [x] Email Review Inbox v1: `/email_reviews`, `email reviews`, `show Gmail reviews`, `what emails need review`, `correos pendientes`, and Catalan variants show grouped pending reviews with human labels and short-lived visible numbers instead of adapter IDs or permanent IDs
- [x] Operator-loop Gmail review visibility: pending/handled Gmail reviews and Gmail-derived action/event counts can appear in attention summaries, daily/start/end-day replies, weekly review/debug context, and planning as non-creatable cleanup guidance
- [x] Email review context actions: after showing the inbox, `show 1`, `approve 1`, `reject 2`, `approve all job-search reviews`, `reject all Endesa reviews`, `reject all the rest reviews from Endesa`, and `turn 3 into an action tomorrow` resolve only against the visible pending review context and expire safely
- [x] Email Review Inbox visible numbers now follow display order across groups; `show 1` and `reject 5` resolve to the number the user saw
- [x] Bulk email review operations re-check current DB status and only mutate still-pending visible reviews. Already approved, rejected, action-converted, or missing reviews are reported separately as already handled.
- [x] Gmail sync replies include pending email review counts and user-friendly new-review/new-event summaries without raw bodies, provider payloads, tokens, ciphertext, IVs, or tags
- [x] Scheduled integration sync can send one bundled Telegram notification when new Gmail review items are created, e.g. grouped job-search/work-action/custom counts; unchanged pending reviews do not produce a new notification
- [x] Scheduled Gmail review notifications respect the Gmail connection review-notification preference. Manual sync still replies in-band.
- [x] Custom Gmail review approval remains review-only by default; explicit `turn N into an action` can create an ActionItem from a visible custom review
- [x] Review semantic dedupe
- [x] Review approval/rejection
- [x] Work-action review approval creates ActionItems instead of Events
- [~] Gmail OAuth/account management remains local-MVP; production auth, key management, and secret rotation are not implemented
- [~] LLM email classifier and semantic router are optional and post-processed by strict allowlists/executors; broader natural-language coverage has targeted API smoke tests but still needs a larger eval suite before production
- [x] ~~Conversation Orchestrator v2 modularization~~ — the `context`, `operation-catalog`, `operation-planner`, `operation-validator`, `operation-executor`, and `response-composer` modules under `apps/api/src/conversation/` have been deleted along with the rest of v2 (see the retirement entry above). `apps/api/src/conversation/gmail-autonomy.ts` and `email-rule-selection.ts` remain — they were never v2-specific. Message route registration stays at `apps/api/src/routes/messages.ts`. `apps/api/src/server.ts` remains oversized and still needs capability executors/services extracted (now targeting Agent Runtime v3's tool catalog, not v2's operation catalog).
- [x] Editing filters on an existing custom Gmail rule through API conversation routing; destructive removal still requires confirmation
- [ ] Gmail send/label modification
- [ ] Gmail webhooks, full-inbox/all-mail LLM monitoring, per-rule sync schedules, business-hours Gmail checks, and daily Gmail digest
- [ ] Additional planned adapters: finance receipts, learning deadlines, custom goal email signals

## Known Stubs Or Deferred Work

- [~] LLM weekly review support exists as guarded/fallback behavior; deterministic weekly review is the reliable path
- [ ] WhatsApp, OpenClaw, web UI, dashboard, mobile app
- [ ] Private GitHub support
- [ ] Full production OAuth/account management, key management, and secret rotation
- [ ] Vector DB/embeddings
- [ ] Billing, enterprise, multi-user product hardening beyond Telegram user mapping
