# Agent Runtime v3 — Global Readiness Audit

Audit date: 2026-08-19. Scope: everything Agent Runtime v3 can and cannot do end-to-end through normal Telegram chat, as of the Daily-Loop Settings V3 Migration (the sixth capability-migration pass — see `docs/09-architecture-inventory.md`'s per-capability migration sections). This is an audit + test-expansion pass only: no product behavior changed, no legacy code deleted. Sources: `apps/api/src/agent-runtime/{tool-catalog,validator,executor,planner,runtime,response-composer}.ts`, `apps/telegram-bot/src/{index,agent-runtime-routing}.ts`, `apps/api/src/server.ts`, `apps/api/src/legacy/*`, `docs/09-architecture-inventory.md`, and every `tests/agent-runtime-*.test.ts` file.

## 1. Capability matrix

"Tests" values: **unit** = a dedicated `tests/agent-runtime-*.test.ts` file, **scripted** = covered by a `tests/agent-runtime-scripted-smoke.test.ts` scenario, **none** = no Agent Runtime v3 test exists. No capability in this table has been smoke-tested against a real, live Telegram bot — every test listed runs through `server.inject("/agent/message")`, the same HTTP surface Telegram calls, but not an actual Telegram conversation.

| Capability | V3 chat | Legacy | Slash cmd | Tests | Mutates | Confirmation | User-ready | Notes/gaps |
|---|---|---|---|---|---|---|---|---|
| Memory save/recall | Yes | Yes | Yes (`/memory`, `/remember`, `/forget_memory`) | scripted (5, 9) | `memory.create` yes / `memory.search` no | No | **Yes** | Recall is grounded — verified a saved memory is retrievable by a later, differently-worded question. |
| Goals list (read-only) | **Yes** (closed — see §11) | Yes | Yes (`/goals`) | unit (7) + scripted (10) | No | No | **Yes** | `goal.list` shows real active goal titles/category/priority/why. See §11. |
| Goals create/update/delete | **No** (by design) | Yes | Yes (`/create_goal`, `/goal_priorities`, ...) | unit (7) + scripted (10) | n/a | n/a | **N/A — honestly declined** | Deliberately still not built (this task explicitly scoped to listing only). The planner now explains this honestly instead of silently substituting `memory.create` — see §11. Progress logging (CVs sent, workouts) still works via `event.log_*`, still not tied to a specific goal record. |
| Actions create/list/complete/snooze/archive | Yes | Yes | Yes (`/action`, `/actions`, `/complete_action`, ...) | scripted (4, 11) | Yes (except list) | No | **Yes** | Full CRUD works; unresolved references ("mark it done" with no id) resolve against the single visible action entity, or ask for clarification if 0 or 2+ match. |
| Reminders (distinct from actions) | n/a | n/a | n/a | n/a | n/a | n/a | n/a | No distinct Reminder concept exists anywhere in the system — "reminder" is UI copy for action due dates and daily-loop nudges. `ActionItemReminderLog` is an internal notification-dedup log, not a user-facing object. Not a gap; there is nothing to migrate. |
| Event logging/progress logging | Yes | Yes | Yes (`/log_progress`, `/events`) | scripted (10) | Yes | No | **Yes** | `event.log_job_applications`/`log_workout`/`log_custom_progress`. |
| Today/operator summary | Yes | Yes | Yes (`/today`, `/review`) | scripted (1, 5, 6, 11) | No | No | **Yes** | Reports open-task count, due-today count, active-goal count, pending-Gmail-review count — all real, no invented figures. |
| Action hygiene | Yes | Yes (still, unmigrated free-text parser) | Yes (`/action_hygiene`) | unit (11) + scripted (4) | Yes (apply) | No (numbered-list resolution instead) | **Yes** | Migrated in an earlier pass; legacy's own free-text parser still backs the slash command. |
| Next-week planning | Yes | Yes | Yes (`/plan_next_week`) | unit (35) + scripted (1, 2, 3) | Yes (apply) | Yes (propose → edit → confirm loop) | **Yes** | Largest and most-tested capability. |
| Weekly review | Yes | Yes | Yes (`/weekly`, `/weekly_last`) | unit (9) + scripted (6) | Yes (save) | Yes | **Yes** | |
| Gmail status/rule listing | Yes | Yes | Yes (`/my_email_rules`) | scripted (5, 7) | `rule.create` yes | `rule.create` yes | **Yes** | |
| Gmail rule pause/resume/archive | Yes | Yes (immediate, no confirm) | Yes (`/pause_email_rule`, ...) | unit (11) + scripted (7) | Yes | Yes | **Yes** | V3 deliberately requires confirmation where legacy doesn't — a safety tightening, not a gap. |
| Gmail reviews/inbox listing | **Yes** (closed — see §10) | Yes | Yes (`/email_reviews`) | unit (11) + scripted (12) | No | No | **Yes** | Originally partial (historical record above superseded). Closed in a follow-up pass: `planner.ts` now has explicit guidance, and the summary is itemized with real subject/sender/snippet, not a bare count. See §10. |
| Email-review → action conversion | **Yes** (closed — see §10) | Yes (`/approve_email_review`) | Yes | unit (11) + scripted (12) | Yes | No | **Yes** | Originally partial. Closed via deterministic index/ref resolution against visible `gmail_review` entities — see §10. |
| Daily-loop settings | Yes | Yes (immediate, no "off" support) | Yes (`/daily_loop_settings`, `/set_daily_loop`) | unit (10) + scripted (8) | Yes | Yes | **Yes** | Most recently migrated capability (this session, prior pass). |
| Guardrails/risk responses | **Yes** (generic, goal-aligned — see §9) | Yes (hard-coded, gambling/trading-only, LLM-classified) | n/a | unit (10) + scripted (13, 14, 15) | `memory.create` (risk log) only | n/a | **Yes** | Originally the headline gap in this audit (§5, preserved below as historical record). Closed in a follow-up pass by a generic goal/guardrail conflict engine (`apps/api/src/agent-runtime/goal-guardrails.ts`) — not a port of legacy's gambling-specific classifier. See §9. |
| Onboarding/help/setup | **No** | Yes | Yes (`/start`, `/setup`, `/help`) | none via `/agent/message` | n/a | n/a | n/a (covered elsewhere) | Not reachable via V3 chat at all; fully served by dedicated onboarding routes and `/help` (which itself calls `/messages/process`, not V3). Not a blocker — slash commands and first-run onboarding already cover this need without V3's involvement. |
| Proactive behavior | **No** | No | n/a | n/a | n/a | n/a | n/a | Does not exist anywhere in the system yet — confirmed by this audit's own tool-catalog/routing sweep finding zero push-initiated code paths. This is the explicitly-planned next phase this audit exists to gate. |

## 2. Routing reality

**Default Telegram normal-text path (no env vars set):** `bot.on("message:text", ...)` (`apps/telegram-bot/src/index.ts:2065-2093`) calls `isLegacyTelegramChatEnabled()` (`agent-runtime-routing.ts:41-43`), which is `true` only when `TELEGRAM_AGENT_RUNTIME_V3_ENABLED === "false"` exactly. Unset, or set to anything else (including `"true"`), routes to `routeToAgentRuntimeV3(...)` (`index.ts:2077-2092`), which POSTs to `/agent/message`.

**`TELEGRAM_AGENT_RUNTIME_V3_ENABLED=true`:** identical to unset — no behavioral difference.

**`TELEGRAM_AGENT_RUNTIME_V3_ENABLED=false`:** `routeToLegacyMessageProcessor(ctx)` (`index.ts:1997-2063`) runs multi-intent → daily-checkin → ingest-text → `conversation/control` first, falling through to `POST /messages/process` (`index.ts:2054-2057`) only if nothing else claims the message.

**Is `/messages/process` still used by default Telegram traffic?** Yes, in three narrow places even with v3 enabled: the `/help` command calls it directly (`index.ts:136`); a pending-decision-with-trailing-command middleware calls it unconditionally regardless of the v3/legacy flag (`index.ts:55`); and `createManualActionFromCommand` (backing `/action`, `/todo`, `/add_action`) routes risky/gambling-sounding text through it specifically to reuse legacy's risk classifier (`index.ts:3818`) before creating a manual action from a slash command.

**Every slash command (105 registered, `index.ts:96-1965`)** calls a dedicated `/users/:id/...` API route, not `/agent/message` and (with the three exceptions above) not `/messages/process` either. Full one-line-per-command list is in the raw audit research; the pattern is consistent: memory/reflections/notifications/goals/integrations/actions/events/checkins/pending-actions each have their own route, several of which (`/actions/hygiene`, `/weekly-review*`, next-week-plan, Gmail email-rules) still call into `apps/api/src/legacy/*` server-side.

**Remaining live imports of `apps/api/src/legacy/*`** (all from `apps/api/src/server.ts` unless noted): `daily-conversation.ts` (onboarding routes + `messages-process.ts`), `messages-process.ts` (the `/messages/process` handler itself), `action-hygiene-conversation.ts` (`/actions/hygiene` route + `messages-process.ts`), `planning-conversation.ts` (next-week-plan route + `messages-process.ts`), `weekly-review-conversation.ts` (`/weekly-review*` route, `planning/next-week.ts`, `operator/attention.ts`, `messages-process.ts` — already a thin re-export shim from an earlier migration pass), `email-review-conversation.ts` (**only** `messages-process.ts` — the dedicated `/email-reviews*` routes use the separate, non-legacy `email-review-service.ts` instead), `gmail-conversation.ts` (Gmail email-rule slash routes + `messages-process.ts`). No legacy module is currently unreferenced by any live route.

## 3. Scripted smoke suite (expanded)

13 scenarios total in `tests/agent-runtime-scripted-smoke.test.ts` (8 existing + 5 new this pass), covering all ten lettered flows (A–J) from the audit brief:

| Letter | Flow | Scenario(s) |
|---|---|---|
| A | Memory save + recall | 9 (new) |
| B | Goals/progress | 10 (new — documents the goal-listing gap) |
| C | Action CRUD | 11 (new) |
| D | Action hygiene | 4 (existing) |
| E | Next-week planning | 1, 2, 3 (existing) |
| F | Weekly review | 6 (existing) |
| G | Gmail rule management | 7 (existing) |
| H | Gmail reviews / email-to-action | 12 (new — proves the tool mechanics work via a direct mocked plan, documents the missing planner guidance) |
| I | Daily-loop settings | 8 (existing) |
| J | Guardrails | 13 (new — proves both the no-trigger gap and the configured-trigger hard-block) |

All scenarios assert (via the shared `tests/helpers/agent-runtime-scripted-eval.ts` harness): no generic Agent v3 error, no false pre-execution success claims, no mutation before an explicit "yes" where confirmation is required, `pendingOperation`/`visibleEntities` staying in sync turn-to-turn, and final DB state matching the reply's claim. Full suite: **313/313 passing** (297 before this pass + 5 daily-loop-settings scripted/unit tests already landed + 5 new scenarios × ~1 test each — see validation section for the exact breakdown).

## 4. Real-LLM local eval mode

CI stays fully deterministic — every scripted turn above sets an explicit `plan` field, which `mockPlan()` (`tests/helpers/agent-runtime-test-helpers.ts:34`) writes to `AGENT_RUNTIME_PLANNER_MOCK_RESPONSE`, so no network call happens and nothing depends on `OPENAI_API_KEY` in CI.

To run the same conversational shapes against the **real** LLM locally: write (or temporarily edit) a `ScriptedTurn` with **no `plan` field**. `runScriptedScenario` (`tests/helpers/agent-runtime-scripted-eval.ts:97-100`) only calls `mockPlan()` `if (turn.plan)` — omit it, and `planner.ts`'s real `planMessage` runs, which calls the OpenAI API when `OPENAI_API_KEY` is set in your shell, or falls back to the deterministic heuristic planner otherwise (`planner.ts:39-40`, `heuristicPlan`). Concretely:

```bash
export OPENAI_API_KEY=sk-...
export DATABASE_URL=postgresql://postgres:postgres@localhost:5432/alecto_ai?schema=public
pnpm exec tsx --test tests/agent-runtime-scripted-smoke.test.ts
```

Do this on a **copy** of a scenario (e.g. duplicate scenario 9's turns without their `plan` fields) rather than editing the checked-in file permanently — the checked-in scenarios must keep their `plan` fields so CI stays deterministic. No test file changes were needed to enable this; the hook already existed before this pass, just undocumented and unused.

## 5. Guardrails — the headline finding (historical — see §9 for the closed-gap follow-up)

`checkPolicyGuardrail` (`apps/api/src/agent-runtime/validator.ts:22-37`) is deliberately generic and data-driven: it only matches the user's own `UserOperatingProfile.knownTriggers`/`knownFailureModes` (both empty by default), and is checked in `runtime.ts:215-227` **before** the planner runs — if triggered, it returns a fixed "let's slow down and talk it through first" reply, plans and executes nothing.

Legacy has a **separate, more sophisticated, hard-coded mechanism** that V3 does not share at all: `packages/core/src/message-processing.ts:411-422` classifies `betting_intent`/`trading_intent` with an LLM-derived `riskState` (RED/ORANGE/GREEN) and, on RED, returns a hard "No. Hard stop." refusal — escalated further when the profile's distinct `gamblingGuardrails` field (default `"strict"`, escalatable to `"hard_guardian"`) is set. This is real, currently-live behavior — it's why `/action`/`/todo`/`/add_action` still route risky-sounding text through `/messages/process` even with v3 chat otherwise enabled (`apps/telegram-bot/src/index.ts:3818`).

**Net effect (at the time of the original audit):** a user typing "I want to bet €1000 because it feels safe" into normal V3 chat got **no special handling at all** unless they had already, separately, configured "bet"/"gambling" as a `knownTrigger` — something no default user would have done. **This is now closed — see §9.** The product decision was explicitly *not* to port legacy's `betting_intent`/`trading_intent` classifier; instead V3 gained a generic goal-conflict engine that treats "stop gambling" as just one instance of a user-authored goal, no different from "quit smoking" or "train 3x/week."

## 6. Deletion/migration candidates

No legacy module is currently orphaned — every file under `apps/api/src/legacy/` is imported by at least one live route (§2). Nothing was deleted in this pass, per the task's explicit instruction.

**Legacy capabilities fully replaced by V3 (conversationally), but files still load-bearing for other reasons:**
- `action-hygiene-conversation.ts`'s free-text parser — superseded conversationally by `action.hygiene_apply`, but its file/route still backs `/action_hygiene`.
- `planning-conversation.ts`'s opt-in-create parser — superseded conversationally, still backs `/plan_next_week`.
- `weekly-review-conversation.ts` — already a thin re-export shim from an earlier pass; its real logic lives in non-legacy `weekly-review/{context,review}.ts`. This file itself (not its logic) is close to deletable — the remaining callers (`server.ts`'s `/weekly-review*` route, `planning/next-week.ts`, `operator/attention.ts`, `messages-process.ts`) could plausibly import the non-legacy modules directly instead of through this shim.

**Legacy capabilities still genuinely needed by slash commands (not just interop):**
- `gmail-conversation.ts` (Gmail email-rule slash routes have real, non-duplicated logic there).
- `daily-conversation.ts`'s onboarding pieces (no V3 or other non-legacy equivalent exists for first-run onboarding).

**Legacy capabilities only reachable via `TELEGRAM_AGENT_RUNTIME_V3_ENABLED=false` or `/messages/process`'s narrow remaining callers:**
- `email-review-conversation.ts` — its only live caller is `messages-process.ts` itself; the dedicated `/email-reviews*` routes already use the separate `email-review-service.ts`. V3's Gmail-review capability has now moved from "partial" to "yes" (§1, §10), so the remaining blocker for deleting this file is purely the legacy-fallback flag/`/messages/process`'s other callers, not a V3 capability gap anymore.
- `messages-process.ts` itself — cannot be deleted while `TELEGRAM_AGENT_RUNTIME_V3_ENABLED=false` remains a supported escape hatch, and `/help` depends on it. Its gambling/trading-specific risk classifier (`packages/core/src/message-processing.ts`) is now **product-superseded**, not just technically-superseded, by V3's generic goal/guardrail engine (§9) — the plan is not to port it, so this is no longer a reason to keep the file, though the flag/`/help` dependencies still are.

**Candidates to keep as admin/fallback indefinitely:** `messages-process.ts` (as the `TELEGRAM_AGENT_RUNTIME_V3_ENABLED=false` escape hatch) and `gmail-conversation.ts`/`daily-conversation.ts`'s slash-command-serving pieces (no reason to touch working, still-used code paths).

## 7. Early-user readiness verdict

**What Alecto can safely do now, in normal V3 chat:** save/recall memories; log progress and events; full action CRUD plus action-hygiene cleanup; the complete next-week planning propose → edit → confirm loop; weekly review show/save; Gmail connection status, rule listing/creation/pause/resume/archive; daily-loop settings show/change; itemized Gmail review triage (list → convert to action / reject by natural reference, as of §10); real, grounded active-goal listing (as of §11); nudge a genuinely empty user toward setting one real goal/guardrail anchor when they ask a broad operator/help question (as of §12); and, as of the §9 follow-up, intervene when a message conflicts with a goal or configured trigger the user has actually set. All of the above are tested (unit and/or scripted), require confirmation where they mutate meaningfully, and never fabricate facts in their grounded replies.

**What it should not promise yet:** creating/editing/deleting goals via chat (deliberately still unbuilt — §11 closes only the listing/honesty half, and the planner now says so explicitly rather than silently substituting a memory); onboarding/help guidance through normal chat beyond the one-line nudge (still slash/legacy-route only for anything deeper, which is fine, just don't imply chat covers it); any proactive/unprompted behavior (doesn't exist yet, by design — the §12 nudge is a reply to a question the user asked, never unprompted); any Gmail send/reply/label/mailbox mutation of any kind (still, deliberately, entirely unsupported); and protection at all for a risk the user has never named as a goal or trigger (§9) — the §12 nudge softens this by inviting the user to set one, but a user who ignores the nudge is still unprotected. Alecto does not, and by design should not, guess at unstated risks.

**Biggest risks for user testing:**
1. **Ambiguous Gmail-review references collapse to one generic clarification message.** `resolveGmailReviewRef` (§10) can't distinguish "zero matches" from "multiple matches" the way `gmail.rule.propose_update` does — both are safe (never mutate), but the wording is less specific than users may expect.
2. **No goal creation via chat at all, only an honest decline.** A user who says "create a goal to run a marathon" gets a clear, correct "not wired yet, use /create_goal" — this is the intended, safe behavior, not a bug, but it's worth confirming product is fine shipping this level of friction rather than wiring minimal creation next. The §12 nudge makes this friction more visible (it actively points empty users toward `/create_goal`), which raises the stakes slightly on this decision.
3. **The nudge's broad-question detection is a small, conservative allowlist, not a general "is this vague" classifier.** A genuinely empty user phrasing their first message unusually (not matching the ~8 patterns in `BROAD_OPERATOR_QUESTION_RE`) gets no nudge and falls straight to normal, potentially generic-feeling chat. This is the safe failure direction (never over-triggers into a specific flow), but means the nudge's real-world coverage depends on how closely users phrase things like the examples.

**Top 3 blockers before external users:**
1. Consider whether Gmail-review ambiguous/unknown-reference clarification wording (§10) is worth sharpening before wide release — currently safe but generic.
2. Decide whether minimal goal creation (reusing the already-clean `createGoal` service — see §11) is worth adding before external users, given how often "create a goal to X" is likely to come up once goal.list *and* the §12 nudge make goals a visible, actively-discussed part of the conversation.
3. Watch real usage of the §12 nudge's broad-question allowlist and expand it if users commonly phrase the same underlying "I don't know where to start" intent in ways it currently misses.

**Top 3 product improvements after baseline:** proactive operator behavior (the next planned phase — see §12, which this audit series was gating and now clears); minimal goal creation via chat, once product decides the honest-decline friction is worth removing; broader/smarter broad-question detection for the goal-anchor nudge if real usage shows the current allowlist is too narrow.

## 8. Validation

`pnpm typecheck` ✅ · `pnpm build` ✅ · `pnpm test` — **313/313** ✅ (308 before this pass + 5 new scripted scenarios) · `git diff --check` ✅. No product code changed in this pass — only test files and documentation. (Superseded by §9 and §10's own validation for their respective follow-up passes.)

## 9. Follow-up: generic goal/guardrail engine (implemented)

A separate follow-up task closed §5's headline gap — deliberately **not** by porting legacy's hardcoded `betting_intent`/`trading_intent` classifier, but by building a domain-agnostic goal-conflict engine: `apps/api/src/agent-runtime/goal-guardrails.ts`. Product direction: Alecto should have no special-cased "gambling" or "financial" logic anywhere — a "stop gambling" goal, a "quit smoking" goal, and a "train 3x/week" goal are all just user-authored `Goal` rows, and the exact same code classifies conflicts against any of them.

**Design — two tiers, run before the planner (`runtime.ts`), replacing the old `checkPolicyGuardrail`:**
- **Tier 1 (deterministic, no LLM):** literal `UserOperatingProfile.knownTriggers`/`knownFailureModes` substring matching — the exact backward-compatible mechanism from before this pass, now living in `goal-guardrails.ts` instead of `validator.ts`. If a matching active goal shares vocabulary with the trigger phrase, the reply names that goal; otherwise it falls back to the same generic phrasing as before.
- **Tier 2 (LLM, optional/mockable, only runs when Tier 1 found nothing and the user has ≥1 active goal):** classifies the message against the user's own active goals **only** (id/title/why — nothing else, and the prompt explicitly forbids inventing a goal not in that list) into `none` / `soft_warn` / `hard_block` / `ask_clarification`, plus a `pattern` (`active_violation`, `avoidance`, `lapse_admission`). Mocked in tests via `AGENT_RUNTIME_GUARDRAIL_MOCK_RESPONSE` (mirrors the existing `AGENT_RUNTIME_PLANNER_MOCK_RESPONSE` pattern), so CI has zero `OPENAI_API_KEY` dependency — with neither a key nor a mock set, Tier 2 is skipped entirely and the turn defaults to `allow`, which is also why every pre-existing test that happens to seed goals (e.g. next-week-planning's tests) was completely unaffected by this change.
- A returned `goalId` is **never trusted blindly** — it's checked against the actual list of goals passed to the classifier; an invented or mismatched id collapses back to `allow` rather than blocking on a goal that doesn't exist (unit-tested explicitly).
- Reply text is a small set of deterministic templates parameterized by goal title + pattern + the user's own `directness` profile field (blunter phrasing at `directness >= 4`, softer otherwise) — never raw LLM prose, consistent with this codebase's established "LLM proposes, deterministic code composes the grounded reply" rule.
- On `hard_block`/`soft_warn`, the conflict is logged as a `risk_pattern` memory via the existing `createMemory`/`memory.create` mechanism (already read by `insights.ts`/`daily-review.ts`) — no new schema, and the LLM never touches the DB itself; the deterministic runtime code decides whether/what to log after classification.

**Examples now supported, all via the same code path:** "Stop gambling" + "I want to bet 1000 because it's safe" → hard block naming the goal. "Improve financial discipline" + an impulsive-purchase message → hard block naming that goal. "Find a new developer job" + browsing cars instead of applying → soft-warn accountability nudge. "Train 3x/week" + "I skipped gym again" → supportive lapse acknowledgment, not a hard stop. "I want to control my gambling impulses" against the same "Stop gambling" goal → correctly let through as supportive, not treated as the violation. No goal/trigger relevant → normal chat, untouched.

**Tests:** `tests/agent-runtime-goal-guardrails.test.ts` (new, 10 tests, covering exactly the A–H scenarios this follow-up task specified, plus invented-goalId and ask_clarification edge cases) + 3 rewritten/added scripted-smoke scenarios (13: cessation-goal hard-block + supportive follow-up + risk-memory logging; 14: pursuit-goal avoidance; 15: trigger-only path with no goal, confirming Tier 1 still works standalone) + one pre-existing test in `tests/agent-message.test.ts` updated in place, since it asserted the *old* "no mutation" guardrail behavior, which the new risk-logging behavior intentionally changes. Full suite: **325/325** (313 before this follow-up + 10 unit + 2 net-new scripted scenarios).

**Validation:** `pnpm typecheck` ✅ · `pnpm build` ✅ · `pnpm test` (325/325) ✅ · `git diff --check` ✅.

**Remaining, intentional gap:** if the user has no relevant goal or configured trigger, Alecto does not intervene — by design, per explicit product direction ("Alecto should not pretend to enforce a goal the user never set"). This means guardrail coverage is only as good as the goals/triggers a user has actually set, which is a meaningfully different (and more honest) safety posture than legacy's blanket gambling/trading detection, but is not a substitute for it for a user who hasn't configured anything yet — see the updated readiness verdict (§7) for the resulting risk and blocker.

## 10. Follow-up: Gmail review triage reachable via normal chat (implemented)

Closes §1's other originally-partial capability: `gmail.review.list`/`reject`/`to_action` were already fully wired mechanically but unreachable by a real LLM turn. No Gmail OAuth/callback/sync/provider code was touched, and no send/reply/label capability was added — this pass only makes the existing, already-DB-only mutations (reject/to_action) actually reachable through normal chat.

**Planner guidance:** `planner.ts`'s system prompt now has an explicit bullet (previously zero) teaching when to plan all three tools, covering "what emails need my attention?", "what Gmail reviews are waiting?", "show pending email reviews", "anything important in Gmail?" (→ `gmail.review.list`), "turn the recruiter one into a task"/"make the recruiter email an action"/"create an action from the email about X" (→ `gmail.review.to_action`), and "reject/ignore the Endesa one" (→ `gmail.review.reject`) — explicit that both mutating tools reference the most recently shown list by `index` or `ref`, never inventing a `reviewId`.

**Itemized list:** `gmail.review.list`'s summary changed from a bare count ("N email review(s).") to a numbered, grounded list (`apps/api/src/email-reviews/email-review-service.ts`'s new `formatGmailReviewListForChat`/`gmailReviewChatLabel`/`gmailReviewChatDescription`) — each line is the review's own real subject (or sanitized sender, or tracking-rule name as a last resort) plus a safely truncated snippet/evidence excerpt, never an invented summary. Reuses the same non-legacy service module the dedicated `/email-reviews` HTTP route already depends on (`extractSafeSenderLabel`/`truncatePlainText` for PII-safety and length limits), rather than duplicating formatting logic. Each listed item is stored as a `gmail_review`-type visible entity with a 1-based `index`, matching the pattern established for action-hygiene lists.

**Follow-up resolution:** `gmail.review.reject`/`gmail.review.to_action` gained optional `index`/`ref` args (alongside the pre-existing `reviewId`, now optional). A new `resolveGmailReviewRef` in `validator.ts` resolves these **synchronously against `context.session.visibleEntities`** — never a fresh DB lookup (unlike `gmail.rule.propose_update`, since a review's identity here is only meaningful in the context of "the list you just showed me") and never hidden LLM memory. An explicit numeric `index` always wins; free-text `ref` is resolved via `selectEmailRuleCandidate` (`apps/api/src/conversation/email-rule-selection.ts`) — the exact same exact-name/token-overlap/ordinal matcher legacy's own numbered Gmail-rule-selection flow already relies on, reused as-is rather than writing a third near-duplicate fuzzy matcher. Zero or multiple matches both resolve to `needs_clarification` with no mutation; a returned candidate id is always a real visible entity's id, never invented. No review list shown yet in the conversation also asks for clarification rather than guessing.

**A real bug found and fixed during this pass:** `runtime.ts`'s `applyExecutionSideEffects` replaces the *entire* `visibleEntities` list with whatever the current turn's own executed op returns. `gmail.review.to_action`/`gmail.review.reject`'s first draft only returned the entity for the thing that turn changed (the new action, or nothing), which silently wiped visibility of any *other* still-pending review — breaking a very next "reject the other one" that doesn't re-list first. Fixed by having both tools re-fetch and re-include the remaining pending reviews (re-numbered) in their own `entities` array. Caught by the scripted scenario below, which chains exactly that sequence.

**Action conversion (`gmail.review.to_action`):** now calls the existing `createActionItemFromEmailReview` service function (same one the dedicated `/email-reviews` route uses) instead of a bespoke inline action-input construction — this adds real title generation (`buildActionTitle`, extracting an action phrase from the subject/body rather than just reusing the raw subject) and goal-linking, matching "preserve existing service behavior" rather than diverging from it. Still approves the review and links `actionItemId` afterward, unchanged.

**Reject (`gmail.review.reject`):** now calls the existing `rejectEmailReviewForUser` service function, which reports an honest "already X" message if the review was already decided rather than falsely claiming a fresh rejection. Confirmed no mailbox mutation of any kind — both functions only ever write to `EmailReviewItem`/`ActionItem` rows.

**Response grounding:** both mutating tools were added to `response-composer.ts`'s `GROUND_TRUTH_ONLY_TOOLS`, so the reply always states exactly what was created/rejected from the executor's own deterministic summary — never the planner's pre-execution `replyDraft` — matching every other confirm-whitelist/ground-truth-bearing mutating tool in the codebase.

**Tests:** `tests/agent-runtime-gmail-review-triage.test.ts` (new, 11 tests — itemized listing, visible-entity storage with indexes, ref-based resolution for both to_action and reject, index-based resolution, ambiguous and unknown references both asking clarification with zero mutation, no-list-shown-yet clarification, honest empty state, and direct-reviewId passthrough) + scripted-smoke scenario 12 rewritten to the exact triage flow this task specified (seed two reviews → list → convert one by ref → reject the other by ref → list again, asserting the final list reflects real DB state and no legacy Gmail setup wall ever appears). All existing tests, including Gmail rule management, planning, action hygiene, weekly review, daily-loop settings, and the goal-guardrail engine, pass unchanged. Full suite: **336/336** (325 before this follow-up + 11 new unit tests, scenario 12 rewritten in place).

**Validation:** `pnpm typecheck` ✅ · `pnpm build` ✅ · `pnpm test` (336/336) ✅ · `git diff --check` ✅.

**What remains unsupported, unchanged:** Gmail send/reply/label/archive/delete or any other mailbox mutation — no tool, no code path, anywhere in V3 does this, and this pass added none. Ambiguous/unknown Gmail-review references still collapse to one generic clarification message (§7's blocker #3) since `selectEmailRuleCandidate` doesn't distinguish "zero matches" from "multiple matches" in its return type — safe, but less specific than `gmail.rule.propose_update`'s richer wording.

## 11. Follow-up: goal-listing honesty (implemented)

Closes the last pre-proactive-operator baseline gap: V3 had no `goal.*` tool at all, so "what are my goals?" either fell through to `operator.today`'s bare active-goal **count** or, worse, risked silently becoming a `memory.create` that let the user believe a real goal was now tracked. Deliberately minimal per this task's explicit scope — **no goal creation/update/delete was added**, only read-only listing plus an honesty rule for creation requests.

**What already existed and was reused as-is:** `getActiveGoals`/`context.activeGoals` (already loaded into every turn's `ContextBundle` by `context-loader.ts`, unchanged); the `Goal` record shape (`title`, `category`, `priority`, `why`, `status`, `createdAt` — no built-in progress/metric aggregation, so none was invented); and `createGoal` (`@operator-agent/db`), which **is** already a clean, well-typed service (confirmed while mapping this task) — deliberately not wired into a `goal.create` V3 tool this pass, per the task's explicit "do NOT build full goal management yet" and its own example response, which prefers an honest decline.

**New tool:** `goal.list` (`apps/api/src/agent-runtime/tool-catalog.ts`) — `mutates: false`, no args, read-only. Executor reads `context.activeGoals` directly (zero extra DB calls — it was already loaded every turn) and formats it with a new `formatGoalListForChat` helper in `executor.ts` (same "small pure helper, no new file" pattern as the daily-loop-settings pass, since — unlike Gmail reviews — there was no existing non-legacy goal-formatting service to extend). Output: a numbered list of `title — category — priority`, with an indented `Why: ...` line when the goal has one, always closing with the same honest boundary line ("Goal editing through chat is not wired yet. Use /create_goal or tell me if you want me to remember context."). Empty state: "You don't have active goals set yet. Use /create_goal for now, or tell me what you want to work on and I can remember the context." — never a false zero-count claim dressed up as something else.

**Planner guidance — two halves:** (1) `goal.list` is taught for "what are my goals?", "show my active goals", "what am I working on?", "which goals are active?", "why did I set the X goal?". (2) An explicit creation/edit request ("create a goal to X", "update my X goal", "delete the X goal") must plan **zero operations** with an honest `replyDraft` pointing to `/create_goal` — explicitly forbidden from using `memory.create` as a silent substitute, since that would let the user believe a goal now exists when it doesn't. A third, narrower case handles genuinely ambiguous statements of intent that are NOT an explicit creation request (e.g. "I want to find a developer job") — `memory.create(type: "goal_context")` is still allowed there (unchanged from the prior pass), but the `replyDraft` is now required to be explicit that this is only being remembered, never framed as if a goal was created or is being tracked.

**Response grounding:** `goal.list` is `mutates: false`, so it already flows through `response-composer.ts`'s existing informational-tools branch — its own grounded summary always wins over `replyDraft`, no composer changes needed.

**Tests:** `tests/agent-runtime-goal-listing.test.ts` (new, 7 tests — real goal titles returned, priority/category/why included, honest empty state, explicit creation request creates neither a Goal row nor a substitute memory, ambiguous intent never lets the reply claim a real goal exists, progress logging (`event.log_job_applications`) still works unaffected, and the goal-aligned guardrail engine — §9 — still sees `context.activeGoals` correctly after this change) + scripted-smoke scenario 10 rewritten to chain exactly what this task specified: two seeded goals → "what are my goals?" (titles appear) → "I sent 3 CVs today" (event logged) → "create a goal to run a marathon" (zero fake goal, zero fake memory). All existing tests, including Gmail review triage, the goal-guardrail engine, planning, action hygiene, weekly review, and daily-loop settings, pass unchanged. Full suite: **343/343** (336 before this follow-up + 7 new unit tests, scenario 10 rewritten in place).

**Validation:** `pnpm typecheck` ✅ · `pnpm build` ✅ · `pnpm test` (343/343) ✅ · `git diff --check` ✅.

**Remaining, intentional gap:** goal creation/update/delete through chat is still entirely unsupported — by explicit task scope, not an oversight. `createGoal` is confirmed clean and ready to wire up if/when product decides the honest-decline friction is worth removing (§7's blocker #3). The other pre-proactive-operator decision point — an onboarding nudge toward setting at least one guardrail-relevant goal, since §9's guardrail engine protects nothing for a user with none configured — is now closed by §12.

## 12. Follow-up: goal-anchor nudge for empty users (implemented)

Closes the pre-proactive-operator onboarding decision §9 and §11 both left open: the goal-aligned guardrail engine has nothing to work with for a genuinely empty user (no active goals, no `knownTriggers`/`knownFailureModes`). Explicitly **not** full onboarding, **not** goal creation, and **not** proactive/scheduled push behavior — it is a small, deterministic, pre-planner check that only ever replies to a broad operator/help question the user asked themselves.

**Where it lives:** `apps/api/src/agent-runtime/runtime.ts`, right before the planner call — the same architectural slot as the goal-aligned guardrail check (§9) and the exact confirm/cancel whitelist: a deterministic short-circuit that either intercepts with a fixed, grounded reply or falls through to normal planning completely unchanged. `shouldShowGoalAnchorNudge(context, message)` returns true only when **all** of: `context.activeGoals.length === 0`; `context.operatingProfile.knownTriggers`/`knownFailureModes` are both empty; `context.session.pendingOperation` is null (never interrupts an in-progress flow); and the trimmed message exactly matches `BROAD_OPERATOR_QUESTION_RE`, a deliberately narrow **allowlist** (not a blocklist) covering "what should I do (today)?", "what can you help (me) (with)?", "help me get organized/started", "so what today", "how do I start/get started/begin", "where do I start", "what now", "what's next" — never a specific-capability request, which always reaches the planner unchanged.

**No new schema:** "was the nudge just shown" is checked against the *existing* `session.messages` history (already persisted, already capped at 20 entries) — `wasGoalAnchorNudgeShownLast` walks backward for the most recent assistant message and checks it for a marker phrase baked into the nudge's own text ("one real goal or guardrail"). If the nudge was the very last thing shown, the next broad-question turn falls through to normal planning instead of repeating — a simple, one-turn suppression, not a persistent cooldown, matching the task's literal "does not repeat immediately" requirement without inventing anything heavier.

**Reply text:** short, matches the task's own example almost verbatim — states the anchor requirement, lists four illustrative example goals (not derived from any real user data — there is none for an empty user), and closes with the same honest boundary line goal.list (§11) already uses ("Goal creation through chat isn't wired yet, so use /create_goal for now — or tell me the context and I'll remember it").

**A real, necessary side effect:** three pre-existing tests (`tests/agent-runtime-scripted-smoke.test.ts` scenarios 5 and 11, `tests/agent-message.test.ts`'s "operator.today never recommends a slash command" test) sent "so what today"/"what should I do today?" from a user with zero goals seeded — which, under the new intended behavior, is exactly the case the nudge is supposed to intercept. Each was updated to seed one realistic goal, preserving what each test actually verifies (memory/Gmail/action-CRUD/operator.today output shape) while accurately representing a non-empty user, the same "update, don't work around" pattern used for the pre-existing guardrail test in §9.

**Tests:** `tests/agent-runtime-goal-anchor-nudge.test.ts` (new, 7 tests — empty user gets the nudge for two different broad phrasings; a user with an active goal does not get it; a user with only a `knownTrigger` (no goal) does not get it, since the guardrail engine already has an anchor; a specific capability request from an empty user does not get it; `goal.list`'s own honest empty state wins over the nudge for "what are my goals?"; the nudge does not repeat on the immediately-following broad question) + scripted-smoke scenario 16 (empty user → nudge → immediate second broad question → no repeat → seed a goal → normal `operator.today` behavior resumes). All existing tests, including goal listing, Gmail review triage, the goal-guardrail engine, planning, action hygiene, and weekly review, pass unchanged (after the three necessary fixture updates above). Full suite: **351/351** (343 before this follow-up + 7 new unit tests + 1 new scripted scenario).

**Validation:** `pnpm typecheck` ✅ · `pnpm build` ✅ · `pnpm test` (351/351) ✅ · `git diff --check` ✅.

**This clears the path to the V3 Proactive Operator MVP.** Every capability-tool gap and the onboarding-anchor product question this audit series tracked are now closed or explicitly, honestly scoped out. Recommended next step: **V3 Proactive Operator MVP** — the phase this entire audit series (§§1–12) existed to gate.

## 13. V3 Proactive Operator MVP (decision/preview layer — implemented)

The first proactive layer for Agent Runtime v3: a pure, callable decision module plus a preview-only API route. **Actual scheduled sending is deliberately not wired this pass** — this is explicitly the "first milestone: preview decisions are correct" the task scoped, not a live feature.

**Existing infrastructure mapped first (nothing here duplicates it):** `apps/worker` already runs a full proactive system — a `setInterval` tick loop (`apps/worker/src/index.ts`) that already sends real Telegram messages on a schedule for legacy daily-loop start-day/end-day, check-ins, insights, action reminders, and Gmail sync notifications, all via a raw `sendTelegramMessage` HTTP call to the Telegram Bot API (no grammy/apps/telegram-bot involvement) and deduped via `NotificationLog` (`userId+type+sentForDate`, calendar-day granularity) or `ActionItemReminderLog` (action-item-specific, not general-purpose). Crucially, **all of that is legacy-content-driven** (calls `/daily-loop/start-day` etc.) and entirely disconnected from Agent Runtime v3's tool catalog/planner/executor — V3 itself had zero proactive code path before this pass, confirmed by this same audit's earlier findings. `apps/api/src/operator/attention.ts`'s `buildOperatorAttentionState`/`generateDailyOperatorBrief` is the closest existing "what does the user need to know" engine, but it's pull-based (used by `/today`/`/operator-attention`) with no send logic of its own.

**What was built:** `apps/api/src/operator/proactive.ts` — `decideProactiveOperatorMessage(input)`, a pure function (no DB reads, no sends) that takes an already-loaded `ContextBundle` (the exact same shape `agent-runtime/context-loader.ts` builds for every normal chat turn — reused as-is, not reinvented), the user's `NotificationSettings`, the current time, and which dedupe keys/how many proactive sends already happened today (both computed by the caller, not the module itself). Returns either `{decision: "no_message", reason}` or a fully-formed `{decision: "proposed_message", type, title, message, reasons, suggestedReplies, dedupeKey, priority, safeToSend}`. Wired to exactly one integration point: `GET /users/:userId/operator/proactive/preview` (`apps/api/src/routes/agent.ts`) — loads context via the unmodified `loadContext`, checks `hasNotificationLog` (existing, unmodified DB function) for each candidate dedupe key, and returns the decision. **Never calls `createNotificationLog` or sends anything** — a repeated preview call shows the same candidate every time, which is correct for a preview-only endpoint.

**Three proactive moments, in priority order (1 highest) when more than one is simultaneously eligible — only one is ever returned per call:**
1. **`morning_brief`** — triggers within ±30 minutes of `morningTimeMinutes`. Grounded top-3 open actions (ranked by priority then due date, never invented), one "skip X" line for a genuinely lower-priority deferred item, and one "watch out" line only when a real `risk_pattern` memory (from the goal-guardrail engine, §9) was logged that same local day. With zero active goals AND zero open actions, reuses the exact goal-anchor-nudge text (`runtime.ts`'s `GOAL_ANCHOR_NUDGE_REPLY`, now exported) rather than sending an empty brief.
2. **`evening_checkin`** — triggers within ±30 minutes of `eveningTimeMinutes`. Only asks about active goals that have a real `targetMetrics[].eventType` AND no matching event logged that local day (checked against `context.recentEvents`) — capped at 2 goals per message, and returns `no_message` entirely if every trackable goal already has a signal today (no nagging).
3. **`gmail_nudge`** — fires whenever `context.gmailReviews` is non-empty and the current time falls between the user's morning/evening bounds (the quiet-hours proxy — see below). Picks the top pending review, summarized via the exact same `gmailReviewChatLabel`/`gmailReviewChatDescription` helpers the Gmail-review-triage pass (§10) already built — safely truncated, sender-sanitized, no raw email address ever included. Deduped **per review** (`v3_gmail_nudge:<reviewId>`), not just per day, so a new review later in the day still gets its own nudge.

**Anti-spam / quiet hours:** no dedicated quiet-hours field exists anywhere in the schema (confirmed during infra mapping) — daily-loop settings are the available proxy, used two ways: `dailyLoopEnabled: false` suppresses every proactive message outright (the clearest available "don't nudge me" signal), and `gmail_nudge` is bounded to the user's own configured morning-to-evening window so it never fires overnight. The 1–3/day cap is a single check — `sentCountToday >= 3` returns `no_message` before any candidate is even built, so it's a hard global cap rather than a "higher priority overrides the cap" mechanism (a deliberate simplification, noted below). Dedup reuses `NotificationLog` exactly as-is, no schema changes — `morning_brief`/`evening_checkin` use fixed per-day keys, `gmail_nudge` encodes the review id into the `type` string for per-item dedup within the same generic table.

**Tests:** `tests/agent-runtime-proactive.test.ts` (new, 11 tests, through the preview route end-to-end) covering all 10 specified unit scenarios (grounded morning brief, goal-anchor fallback, no invented facts, evening check-in asking about untracked signals, no nagging once logged, safe Gmail summary, no-reviews-no-nudge, duplicate-dedupe-key suppression, quiet-hours/daily-loop-disabled suppression, daily-cap suppression) plus 2 new scripted-smoke scenarios (17: preview an evening check-in → user replies with both signals → both events logged via **existing, unmodified** event-logging tools; 18: preview a Gmail nudge → user replies "turn it into a task" → **existing, unmodified** Gmail review-to-action path creates the grounded action) — proving the proactive layer's job is only to decide *whether* to nudge, never to reimplement reply-handling. Full suite: **364/364** (351 before this pass + 11 unit tests + 2 scripted scenarios).

**Validation:** `pnpm typecheck` ✅ · `pnpm build` ✅ · `pnpm test` (364/364) ✅ · `git diff --check` ✅.

**What remains before enabling for real users:**
1. **Actual sending is not wired.** No cron/worker integration calls `decideProactiveOperatorMessage` and no code path calls `sendTelegramMessage`/`createNotificationLog` for a V3 proactive message — by design, per this task's "prefer a callable decision module first" / "do not send real messages automatically" rules. Wiring this means either extending `apps/worker`'s existing tick loop to also call the preview logic (and, on `proposed_message`, actually send + log) behind an explicit enable flag, or a new dedicated proactive worker tick — a genuinely separate, later decision.
2. **Session-entity delivery isn't built.** For a real Gmail-nudge send to support a bare "turn it into a task" reply (no "the recruiter one" qualifier), delivery would also need to record the referenced review as a visible session entity — scenario 18 simulates this directly via the DB to test the reply-handling half honestly, but no code does this yet.
3. **The daily cap is a hard global cutoff, not priority-aware.** "Do not send a lower-priority nudge if a higher-priority one already exists" is satisfied within a single decision call (only the single highest-priority eligible candidate is ever returned), but the cross-call 1–3/day cap doesn't distinguish "3 gmail nudges already sent, block everything" from "a much more important morning brief is now also eligible" — worth revisiting once real sending exists and this matters in practice.

**Next recommended step:** decide whether to wire actual sending (behind an explicit enable flag, starting with morning_brief only, given it's the highest-priority and best-tested moment) — everything the readiness audit series was gating is now either shipped or a deliberate, documented, product-level choice.

## 14. Real delivery wired for morning_brief only (cautious MVP — implemented)

Closes §13's "what remains before enabling for real users" item 1. Real Telegram delivery now exists for exactly one proactive moment — `morning_brief` — off by default, behind an explicit env flag. `evening_checkin` and `gmail_nudge` remain preview-only, unchanged; the preview route itself is untouched and still never writes `NotificationLog`.

**Where it lives:** a new `apps/worker/src/v3-proactive-delivery.ts`, deliberately **not** added to `apps/worker/src/index.ts` directly — `index.ts` runs its tick loop (`await runTick(); setInterval(...)`) as a side effect of being imported, which would make it unsafe to import in tests (would immediately hit the real DB, require `TELEGRAM_BOT_TOKEN`, etc.). This mirrors the existing `integration-sync.ts` pattern exactly (dependency-injected `apiGet`/`sendTelegramMessage`, real `@operator-agent/db` calls used directly since tests already run against a real Postgres test DB throughout this repo). Three small shared datetime helpers (`formatLocalTime`/`formatLocalDate`/`formatMinutesOfDay`, plus `getPart`) were extracted from `index.ts` into a new `apps/worker/src/datetime.ts` so both files can use the identical logic without either duplicating it or importing the side-effecting `index.ts`.

**Flow:** `runV3ProactiveMorningBriefs(settings, options)` is called from `apps/worker/src/index.ts`'s existing `runTick()`, right after the legacy `runDailyMorningBriefs`/`runDailyEveningReviews` calls, reusing the exact same `getUsersWithEnabledNotifications()` settings list already fetched that tick — no new DB query. For each user with `telegramUserId` set, `dailyLoopEnabled` true, allowlisted, and whose local time exactly matches `morningTimeMinutes` (the identical gating `runDailyMorningBriefs` already uses for the legacy morning brief): calls the **existing, unmodified** `GET /users/:userId/operator/proactive/preview` via `apiGet` (same call shape `maybeSendDailyInsight`/`maybeSendWeeklyInsight` already use to fetch content from apps/api). If the decision is `proposed_message` **and** `type === "morning_brief"`, sends via the **existing, unmodified** `sendTelegramMessage` raw Telegram HTTP call, then — only on a successful send — writes `NotificationLog` with the decision's own `dedupeKey` via the **existing, unmodified** `createNotificationLog`. Any other decision (`no_message`, `evening_checkin`, `gmail_nudge`) or a preview-fetch failure is a silent no-op for that user this tick. A send failure is logged and does **not** write `NotificationLog`, so the message stays eligible to retry next tick rather than being falsely marked delivered.

**Flags:**
- `PROACTIVE_OPERATOR_DELIVERY_ENABLED` (env, default unset/off) — the master switch; mirrors the existing `INTEGRATION_SYNC_ENABLED === "true"` pattern exactly, the only precedent for this kind of flag in the codebase.
- `PROACTIVE_OPERATOR_ALLOWLIST` (env, optional, comma-separated userIds) — no allowlist mechanism existed anywhere in the codebase before this pass (confirmed during infra mapping), so a simple new one was added: unset/empty means every `dailyLoopEnabled` user is eligible once the master flag is on; set means only listed userIds are. Intended for a cautious "just my own user" rollout per the task's manual smoke checklist.

**Tests:** `tests/worker-v3-proactive-delivery.test.ts` (new, 9 tests) — disabled flag sends nothing; enabled + real candidate sends exactly one Telegram message; `NotificationLog` written only after a successful send, with the decision's own `dedupeKey`; a failed send never writes `NotificationLog`; an already-logged dedupe key (simulating a prior send) suppresses the send; an `evening_checkin` candidate and a `gmail_nudge` candidate are each confirmed (via a same-tick sanity assertion against the real preview route) to be offered by the decision layer yet never sent, proving the type-filter actually does its job rather than the test setup coincidentally never producing them; the preview route itself still never writes `NotificationLog` across repeated calls; and an allowlist correctly delivers to one user while skipping another in the same tick. All tests call the real, unmodified preview route in-process via `server.inject` (not real network) and stub only `sendTelegramMessage` — no test hits the real Telegram API. Full suite: **373/373** (364 before this pass + 9 new tests).

**Validation:** `pnpm typecheck` ✅ · `pnpm build` ✅ · `pnpm test` (373/373) ✅ · `git diff --check` ✅.

**Manual smoke checklist (for a human to run against a real bot token — not executed as part of this pass, since no test may hit real Telegram):**
1. Set `PROACTIVE_OPERATOR_DELIVERY_ENABLED=true` and `PROACTIVE_OPERATOR_ALLOWLIST=<your own userId>` in the worker's environment.
2. Ensure your user has `dailyLoopEnabled: true` and a `morningTimeMinutes` a minute or two in the future.
3. Start (or let the running) worker tick past that time.
4. Confirm exactly one morning-brief Telegram message arrives, matching the preview route's own output for the same `now`.
5. Wait for the next tick (or force one) — confirm no duplicate arrives (`NotificationLog` dedup).
6. Set `PROACTIVE_OPERATOR_DELIVERY_ENABLED=false` (or remove your userId from the allowlist) — confirm no further sends, even past another morning-time trigger the next day.

**What remains unchanged:** `evening_checkin` and `gmail_nudge` are still preview-only — no code path sends either. The preview route's own behavior, response shape, and no-mutation guarantee are exactly as before. Agent Runtime v3 normal chat routing, slash commands, and `/messages/process` were not touched.

**Next recommended step:** run the manual smoke checklist above against a real bot token for one real user, evaluate the morning-brief UX in practice (message length, timing accuracy, whether the goal-anchor-nudge fallback reads well as an unprompted message rather than a reply), and only then decide whether to expand delivery to `evening_checkin`/`gmail_nudge` or widen the allowlist.

## 15. User-level proactive consent layer (product correction — implemented)

Product correction: `PROACTIVE_OPERATOR_DELIVERY_ENABLED`/`PROACTIVE_OPERATOR_ALLOWLIST` are — and were always meant to be — **developer rollout controls**, not the product UX. Final Alecto should not require an env variable to decide whether to nudge a real user. This pass adds the actual product-level control: three new, dedicated, per-user `NotificationSettings` booleans — `morningBriefEnabled`, `eveningCheckinEnabled`, `gmailNudgeEnabled` — all defaulting to `false` (opt-in, never on by default), plus V3 conversational tools to view/change them, plus the rule that morning-brief delivery now requires this consent in addition to the existing env/allowlist gates.

**Settings model chosen, and why not `dailyLoopEnabled`:** mapping confirmed `dailyLoopEnabled` is a genuinely different, older feature — it (and `morningTimeMinutes`/`eveningTimeMinutes`, which stay shared/reused since they're just "what time is your morning/evening") gates the legacy `/daily-loop/start-day`|`end-day` messages, not V3's proactive engine. Reusing it for the new consent would have been exactly the "overload a vague setting" the task warned against. The three new fields live on the same `NotificationSettings` table (Prisma migration `20260819224352_add_proactive_operator_settings`, purely additive: three `Boolean NOT NULL DEFAULT false` columns, zero data loss risk) rather than a new model, matching how every other notification toggle (`dailyCheckinEnabled`, `dailyInsightEnabled`, `weeklyInsightEnabled`, `dailyLoopEnabled` itself) already lives there. `packages/core/src/notifications.ts`'s `NotificationSettingsSchema`/`UpdateNotificationSettingsInputSchema` and `packages/db/src/index.ts`'s `toNotificationSettings` mapping were updated in lockstep.

**Two independent gates, deliberately separate layers:**
- `decideProactiveOperatorMessage` (`apps/api/src/operator/proactive.ts`) is **completely unchanged** — still a pure content-decision module, still gated on `dailyLoopEnabled` and the existing dedupe/cap/quiet-hours logic exactly as before. It stays unaware of env flags or the new per-user consent fields; every one of §13's existing tests passes unmodified, by design.
- A new `apps/api/src/operator/proactive-eligibility.ts` (`evaluateProactiveEligibility`) layers real send-eligibility on top, independently checking `PROACTIVE_OPERATOR_DELIVERY_ENABLED`, `PROACTIVE_OPERATOR_ALLOWLIST`, and — new — the decision's own `type` against the matching consent field (`morning_brief`→`morningBriefEnabled`, etc.). Used by the preview route to report `wouldSend`/`blockedBy` (`delivery_disabled` | `not_allowlisted` | `user_not_opted_in` | `quiet_hours` | `dedupe` | `no_candidate` — the last three map from the decision module's own `no_message` reasons). This is informational for preview; it is not the sole authority a real send relies on.
- `apps/worker/src/v3-proactive-delivery.ts`'s actual send gate independently re-checks `morningBriefEnabled` itself (alongside its existing `dailyLoopEnabled`/allowlist/telegramUserId checks) before ever calling `sendTelegramMessage` — defense in depth, not reliant on the preview route's eligibility field being consulted correctly by every future caller.

Both `PROACTIVE_OPERATOR_DELIVERY_ENABLED`/`PROACTIVE_OPERATOR_ALLOWLIST` env-reading functions were extracted from `apps/worker` into `packages/core/src/proactive-operator-env.ts` so `apps/api`'s preview route and `apps/worker`'s delivery code read the exact same two env vars the exact same way, without duplicating the parsing logic.

**V3 conversational controls:** three new tools, mirroring `daily_loop.settings_*`'s established propose→confirm shape exactly — `proactive.settings_show` (read-only), `proactive.settings_propose_update` (opens a pending confirmation, never mutates by itself), `proactive.settings_apply_update` (mutates, but the validator rejects it outright if the LLM ever tries to plan it directly — only the exact confirm whitelist re-executing an already-stored pending operation can reach it, identical to every other settings-apply tool in this codebase). Supports "turn on/stop morning briefs", "check in every evening"/"stop evening check-ins", "turn on/stop Gmail nudges", and "what proactive messages are on?" — each toggle independent of the others. Deliberately does **not** support finer controls like "be stricter with this goal" or "less nudges please" — planner.ts explicitly instructs an honest decline for anything beyond the three on/off toggles, matching this session's established scope-boundary-honesty pattern rather than guessing.

**The suggestion mechanism — no new plumbing:** "Alecto can suggest proactive support when a user describes a daily/recurring goal" is implemented purely as a `planner.ts` prompt-guidance addition, not a new mechanism. When the planner recognizes a clearly recurring goal (e.g. "my goal is to go to the gym every day"), it may plan `proactive.settings_propose_update` in that same turn — which is, structurally, identical to any other settings proposal: it opens a pending confirmation and mutates nothing until the user replies with an exact "yes", via the same confirm whitelist every other propose→confirm flow in this codebase already uses. No DB mutation is possible without that explicit confirmation; the LLM cannot silently enable anything.

**Tests:** `tests/agent-runtime-proactive-settings.test.ts` (new, 8 tests — propose/apply for on and off, independent per-toggle mutation, real-settings show, no-pending "yes", unspecified-field clarification, direct-apply rejection), `tests/agent-runtime-proactive-suggestion.test.ts` (new, 2 tests — a suggestion opens a pending confirmation and mutates nothing until confirmed; an ordinary non-recurring goal statement is not forced to trigger a suggestion), `tests/agent-runtime-proactive-eligibility.test.ts` (new, 6 tests — all-clear `wouldSend: true`; each of the three blockers reported independently; the decision content itself is unaffected by opt-in status; the preview route still writes nothing across repeated calls), plus one new fixture-required test in `tests/worker-v3-proactive-delivery.test.ts` (`morningBriefEnabled: false` suppresses a send even with delivery enabled and the user allowlisted) and scripted-smoke scenario 19 (a full, realistic conversation: goal statement → suggestion → confirm → real-settings check → toggle off → confirm, verified against final DB state). All existing tests, including the pure decision module (§13) and morning-brief delivery (§14), pass unmodified. Full suite: **391/391** (373 before this pass + 8 + 2 + 6 + 1 new tests + 1 new scripted scenario).

**Validation:** `pnpm typecheck` ✅ · `pnpm build` ✅ · `pnpm test` (391/391) ✅ · `git diff --check` ✅.

**What this closes:** no proactive delivery is now possible for any user without their own explicit, confirmed opt-in — the env flag and allowlist remain exactly what they always should have been: a developer safety net for controlled rollout, layered underneath, never a substitute for real user consent.

## 16. Settings/delivery UX bugs from real Telegram testing (bug-fix pass — implemented)

Real Telegram testing of §15's consent layer surfaced five problems: (1) "setup a morning brief at 01:06" only set the time via the legacy `daily_loop.settings_propose_update` tool — it never turned `morningBriefEnabled` on, so nothing was ever scheduled to send, despite the bot replying "Done"; (2) "what proactive messages are on?" showed on/off but not the scheduled time; (3) there was no way to ask "why didn't I get my morning brief?" and get a real answer; (4) asking that question hit the generic settings-comparison no-op reply ("That's already how it's set"); (5) "on" in the settings display conflated per-user opt-in with actual deliverability, which also depends on `PROACTIVE_OPERATOR_DELIVERY_ENABLED`, the allowlist, whether the worker process is running, `dailyLoopEnabled`, the time window, and `NotificationLog` dedupe — none of which the user can see.

**Root cause:** two tools independently, and only partially, controlled the same `NotificationSettings` row — `daily_loop.settings_propose_update` could set `morningTimeMinutes` but not `morningBriefEnabled`; `proactive.settings_propose_update` (§15) could set `morningBriefEnabled` but had no time-setting capability at all. Neither tool could express "turn on AND schedule" as one action, so the planner picked whichever tool matched the surface phrasing and silently dropped the other half of the user's intent.

**Fix — one tool now owns both concerns:** `proactive.settings_propose_update`/`settings_apply_update` gained `morningTimeText`/`eveningTimeText` (propose) and `morningTimeMinutes`/`eveningTimeMinutes` (apply) arguments, alongside the existing enable/disable booleans. A combined request ("set up a morning brief at 9am") now proposes and applies both fields as one fused change — "You're about to turn on the morning brief at 09:00. Reply yes to confirm or cancel." / "Done — the morning brief is now on at 09:00." — rather than two disjointed phrases. A time-only request while the moment is off ("move morning brief to 9") still only changes the time, but the reply now says so explicitly: "Done — the morning brief time is now 09:00, but the morning brief is still off." `proactive.settings_show`'s summary now includes the scheduled time when a moment is on ("Morning brief: on, around 09:00"). The legacy `daily_loop.settings_*` tools are unchanged.

**New diagnostic tool — `proactive.diagnose_morning_brief`:** answers "why didn't I get my morning brief?" / "it's 9 and no morning brief" / "where is my morning brief?" with a single, most-specific, grounded status via `getProactiveDeliveryStatus` (`apps/api/src/operator/proactive-eligibility.ts`), instead of the generic settings-comparison reply. Statuses, checked in order: `delivery_disabled` (env kill switch off) → `user_not_allowlisted` (allowlist set but excludes this user — `missing_allowlist` is kept in the type for vocabulary completeness but is not currently reachable, since an unset allowlist means "open to everyone," not a blocker) → `user_not_opted_in` (the actual per-user consent is off) → `daily_loop_disabled` (the older feature's own gate, still checked by the content-decision module) → `outside_morning_window` → `duplicate_dedupe_key` (already sent today) → `no_candidate` (decision module found nothing grounded to send) → `eligible` ("Settings look eligible — check whether the worker process was running... and whether Telegram delivery failed"). `getProactiveDeliveryStatus` reuses `decideProactiveOperatorMessage` and its exported time-window primitives (`isWithinWindow`, `minutesOfDayInTimezone`, `TIME_TRIGGER_WINDOW_MINUTES`) directly rather than reimplementing them, so it can't drift out of sync with either the content-decision module or `apps/worker`'s own send gate. See the doc comment in `apps/worker/src/v3-proactive-delivery.ts` for exactly how its exact-minute tick trigger and this diagnostic's ±window check relate (same underlying gates, deliberately different time-matching semantics for a continuously-ticking sender vs. a point-in-time explanation) — true single-function sharing isn't possible across the `apps/api`/`apps/worker` package boundary (HTTP-only, no shared source imports), so drift is avoided by both sides importing the same low-level primitives from `proactive.ts`, not by literal code identity.

**Preview route:** `GET /users/:userId/operator/proactive/preview` now additionally reports `scheduledTime` (the morning brief's formatted time, when one is proposed) and `optIn` (the three raw per-user consent booleans), alongside the existing `wouldSend`/`blockedBy`. Purely additive — still computes and returns without writing `NotificationLog` or any other DB state.

**Tests:** 3 new tests in `tests/agent-runtime-proactive-settings.test.ts` (fused enable+time propose/apply, time-only-while-off with the correct caveat text, settings_show includes the scheduled time), a new `tests/agent-runtime-proactive-diagnosis.test.ts` (6 tests — one per reachable status: `delivery_disabled`, `user_not_allowlisted`, `user_not_opted_in`, `daily_loop_disabled`, `duplicate_dedupe_key`, `eligible`), 2 new tests in `tests/agent-runtime-proactive-eligibility.test.ts` (preview reports `scheduledTime`/`optIn`; preview still writes nothing). One pre-existing eligibility test's strict `deepEqual` was loosened to check `wouldSend`/`blockedBy` individually now that the response carries additional fields. Full suite: **401/401** (391 before this pass + 3 + 6 + 2 - 1 test renumbered, net +10 new tests).

**Validation:** `pnpm typecheck` ✅ · `pnpm build` ✅ · `pnpm test` (401/401) ✅ · `git diff --check` ✅.

**What this closes:** a combined "turn on and schedule" request can no longer silently apply only half of what the user asked; the settings display shows the actual scheduled time; and a user asking why a proactive message didn't arrive gets a grounded, specific answer instead of a settings-comparison non-answer — without changing Telegram routing, without broadening who gets sent to, and without touching the env-flag/allowlist developer rollout controls' role.
